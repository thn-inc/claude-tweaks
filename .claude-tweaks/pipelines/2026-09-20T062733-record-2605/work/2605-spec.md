---
record: 2605
origin: harness-health
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
fingerprint: harnesshealth-1e92fbb0
---
# 2605: Skill best-practice: skill-prose-conformance-tests — Anti-Patterns

**Skill:** skill-prose-conformance-tests | **Section:** Anti-Patterns | **Category:** best-practice | **Classification:** additive | **Confidence:** high

Verified-as-of: 9f25890b495ba5bbb1ba2e79fa56e2ad6b9c9371

## Current State

The context-cost bloat scan (bloat.js) flags this row at 678 B against a 216 B corpus median across the 34 Anti-Pattern rows in this file (over the 2x-median flag threshold of 432 B, alongside two other flagged rows at L203/L209). Reading the flagged text confirms it is a resolved-incident summary of what happened in a specific past record (#2022's build and review passes) rather than a reusable constraint — the row's first sentence already states the generalizable rule ('the same literal often exists in more than one concatenated member...a stronger pin...may already exist elsewhere') on its own. Per the bloat-signal guard in _shared/harness-health-analysis.md dimension 9 ('Over-long rows...What is not legitimate is a row that has absorbed the reason it was added — an incident narrative, a summary of what a past change did — which belongs in the project's incident log, not in a table re-read on every invocation'), and docs/incident-log.md carries no #2022 entry, so the narrative isn't even duplicating a canonical home — it exists only in this table.

## Deliverables

**Current:**
```
 `#2022` hit both variants in one record: build's own SCRATCH pin initially matched the wrong template inside the concatenation, and review's Simplify pass then found a newly-authored CALIBRATION byte-identical pin duplicating, more weakly, an existing correctly-scoped one in a sibling test file — grep the suite for the property first
```

**Proposed:**
```
 Grep the suite for the property first.
```

## Acceptance Criteria

The Anti-Patterns row for 'Authoring a new pin against a multi-file concatenated corpus without checking for an existing single-file pin of the same property' has absorbed an incident narrative — naming record #2022's specific build/review sequence — that is not recorded in docs/incident-log.md and is not needed to act on the row's guidance.

_Filed by `/claude-tweaks:harness-health`. Close to resolve; label `wontfix` to suppress future reports of this finding._

<!-- work-fingerprint: harnesshealth-1e92fbb0 -->
work-fingerprint: harnesshealth-1e92fbb0
