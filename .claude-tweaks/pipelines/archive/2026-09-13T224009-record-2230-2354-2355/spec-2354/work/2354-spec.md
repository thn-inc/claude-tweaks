---
record: 2354
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 2354: reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-05T132303-record-1535

Surface: infra

## Current State

Reconcile's automated hygiene sweep flags this pipeline run directory as `structurally-stuck` (`reason: no-branch`) — the cache entry (`.claude-tweaks/reconcile-cache.json`, key `structurally-stuck:{path}`) shows `count: 18`, `lastError: "stuck at no-branch"`, auto-filing this issue per `bin/lib/reconcile`'s escalation mechanism (#644 Deliverable 2 / #1613's visibility layer).

The stuck directory: `.claude-tweaks/pipelines/2026-09-05T132303-record-1535`. Its `run-state.json` stamps `worktree: .claude/worktrees/dispatch-record-1535` and `pr: {number: 1901, branch: worktree-dispatch-record-1535}`.

Investigation confirms the underlying work already shipped cleanly: issue #1535 is closed, PR #1901 merged 2026-09-05T14:40:33Z, and both the worktree and the `worktree-dispatch-record-1535` branch have since been deleted (standard post-merge cleanup) — there is nothing left to build or resolve; the run directory is legitimately done and should have been archived.

Root cause: `bin/lib/reconcile/archive-merged.js`'s `#1962` no-branch fallback probes the PR by number via `resolvePrStateByNumber` when the stamped worktree and its branch are both gone, but only archived when `byNumber.state === 'CLOSED'` — a closed-without-merging PR. A PR that was actually merged reports `state: 'MERGED'`, which that check never matched, so a merged-then-cleaned-up run permanently fell through to the plain `no-branch` skip on every pass — exactly the symptom this issue reports.

## Deliverables

Fix belongs with #2226, and it has already landed and merged to `main` as part of PR #2386 (commit `4c3848121a7af20719a0d04f9462876be476c006`): the `#1962` fallback now also recognizes `byNumber.state === 'MERGED'`, gated by `localHasMerge(root, byNumber.mergeCommit)`. This record does not duplicate that fix's spec.

- Confirmed by direct verification (this build): `gh pr view 1901` reports `state: MERGED`, `mergeCommit.oid: ea622898a8bab17e7a03f3d1afa6a6e8699c5280`.
- Confirmed the merge commit is locally reachable from this checkout's `HEAD` (`git merge-base --is-ancestor ea622898a8bab17e7a03f3d1afa6a6e8699c5280 HEAD` succeeds) — the `localHasMerge` gate the shipped fix applies is satisfied, so archival is not blocked on `local-behind-merge`.
- Confirmed the merged fix's existing regression test (`tests/bin-lib/reconcile/archive-merged.test.js`, "a run dir whose stamped worktree is gone and branch is unrecoverable is archived once its merged PR is confirmed by number and the merge commit is locally reachable") exercises this exact shape.
- No additional code change is needed in `archive-merged.js` for this record — the fix already ships on `main`.
- Once reconcile's next pass runs against the main checkout, `.claude-tweaks/pipelines/2026-09-05T132303-record-1535` archives automatically; if reconcile's cadence doesn't reach it in reasonable time, a human (or `/tidy`) can archive it manually via `node "${CLAUDE_PLUGIN_ROOT}/bin/hooks.js" archive-run --run ".claude-tweaks/pipelines/2026-09-05T132303-record-1535"`.

## Acceptance Criteria

- [x] Confirmed this directory's stuck state is resolved by #2226's fix (shipped as part of #2386) landing — verified via `gh pr view 1901` (`MERGED`, mergeCommit reachable from local `HEAD`) and the existing MERGED-state regression test in `archive-merged.test.js`.
- [ ] `.claude-tweaks/pipelines/2026-09-05T132303-record-1535` is archived (or otherwise resolved) and no longer reported `structurally-stuck` by reconcile — resolves on the next reconcile pass against the main checkout, since the code fix is already live on `main` and the merge commit is locally reachable.

## Technical Approach

No new code change: PR #2386 (merged to `main` prior to this build) already extended `archive-merged.js`'s `#1962` fallback to recognize `byNumber.state === 'MERGED'`, gated by `localHasMerge`. This record's own investigation (PR #1901 state + merge-commit ancestry check, sibling-fix cross-check) confirms the shipped fix covers this exact directory; the stuck directory itself resolves via reconcile's normal sweep cadence, not a build-time code edit.

### Key Files

- `plugin/bin/lib/reconcile/archive-merged.js` — already fixed by #2386; no additional change
- `tests/bin-lib/reconcile/archive-merged.test.js` — already covers this exact scenario (no-branch + MERGED + reachable merge commit, added by #2386)

## Gotchas

- This is one of several `reconcile: structurally-stuck` auto-filed issues sharing the same underlying `#1962`-fallback gap (see also #2226, #2228, #2229, #2230, #2231, #2355-#2359). #2226/#2228/#2229's fix landed as PR #2386 before this record's build ran — confirmed via `git log`/`gh pr view 2386` at build time rather than assumed. A human should also confirm whether #1811 (an earlier, already-`ready`, more comprehensive consolidated fix for this same defect family) already supersedes some or all of these — out of scope to resolve here.
- The near-duplicate screen does not flag these reconcile-residue records against each other by design (title-similarity is deliberately excluded for `reconcile:`-prefixed titles) — this cross-reference is a manual note, not an automated signal.

## Original request

reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-05T132303-record-1535

Reconcile has failed `structurally-stuck` on this path for 3 consecutive passes (first observed 2026-09-12T18:44:23.229Z).

**Path:** `/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-05T132303-record-1535`
**Reason:** `structurally-stuck`
**Last error:** stuck at no-branch

Filed automatically by `bin/lib/reconcile` — see #644.

<!-- fingerprint: reconcile-residue-93fafe99 -->
