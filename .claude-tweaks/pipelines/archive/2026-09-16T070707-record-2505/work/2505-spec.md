---
record: 2505
origin: capture
risk: low
size: medium
ceremony: standard
grants: [build]
---
# 2505: reconcile: structurally-stuck resolve/escalate re-fetch gh issue list once per stuck dir per pass

Defer-reason: genuinely-larger

## Current State

plugin/bin/lib/reconcile/escalate-residue.js:157 — the structurally-stuck fingerprint is reason-scoped (no targetPath), so findResidueDuplicate's uncached gh issue list --repo ... --limit 10000 call re-fetches identical data once per stuck dir per reconcile pass, both from cache.js's pruneResidueFailures loop (which already iterates multiple residueFailures entries in one call) and, more broadly, across every trackResidue/pruneResidueFailures call site in one reconcile run (archive-merged.js, reap-merged.js). Found by the whole-branch pre-release review over 61e936a98..HEAD. Not a quick fix: a narrow fix only inside one loop misses the cross-call-site case, and threading a shared cache across cache.js, archive-merged.js, reap-merged.js, and escalate-residue.js (4+ files) to cover the whole reconcile pass is a real design decision about cache lifetime/invalidation, not a mechanical change.

## Deliverables

Design and implement a shared per-reconcile-pass cache (or equivalent) for findResidueDuplicate's gh issue list call, scoped correctly across every call site active in one reconcile pass.

## Acceptance Criteria

A single reconcile pass touching N stuck dirs/paths makes at most one gh issue list --limit 10000 call for a given repo, not N.

No change to which duplicates are detected or how dedup-hit/append/reopen decisions are made.

Cache is correctly scoped to one reconcile pass (no stale cross-pass reuse).

_Filed by `capture` via specShapedBody._
