---
record: 2321
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 2321: Re-pin tests/impeccable-cli-contract.test.js to the installed Impeccable (4.x) or reinstall the pinned 3.6.0

**Related:** #2314

Origin: ledger resolve gate

Defer-reason: needs-human-decision

## Current State

During the #2251 run, `tests/impeccable-cli-contract.test.js` began failing mid-run: the installed Impeccable version resolved to 4.1.0 while the repo pins 3.6.0 (`tools/upstream-drift`). The same full suite passed twice earlier in the run (8234/0), so the install changed underneath the session — not a regression from any branch. Until resolved, every `npm test` on this machine reports one failure that has nothing to do with the diff under test.

## Deliverables

Maintainer decides: re-record the contract fixtures against the installed 4.x via the `upstream-drift` procedure and move the pin, or reinstall 3.6.0 (`npm install -g impeccable@3.6.0`).

Record the decision beside the pin (the test file header or the manifest).

Confirm which install the test resolves on this machine (the global npm list is empty and no `impeccable` binary is on PATH, yet the test read 4.1.0) and note the resolution path in the test header.

## Acceptance Criteria

`node --test tests/impeccable-cli-contract.test.js` passes 6/6 on this machine.

The pin and the installed version agree, with the decision recorded.

_Filed by `ledger resolve gate` via specShapedBody._


---
Filed by the multi-spec wrap-up of run `2026-09-11T204239-spec-2251-2252-2253-2254-2255-2256-2258` (queue write from spec 2251; source `spec-2251/staged/ledger-record-impeccable-cli-contract-repin.md`; consoleAutoResolve at `autonomy: unattended`).

https://claude.ai/code/session_018rz67jb18j1RLSqhjEdYWH


