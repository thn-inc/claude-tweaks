---
record: 1753
origin: capture
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 1753: _shared/worktree-setup.md: Adopt-or-create section restates the fetch+merge point twice

Surface: infra

## Current State
`plugin/skills/_shared/worktree-setup.md`'s Adopt-or-create section, "Already isolated" branch, states the same fact in two consecutive sentences. The first says, of the adopt path, that "no `EXPECTED_BASE` capture applies … see Post-creation catch-up's own 'no `EXPECTED_BASE` to capture' case, which already documents skipping just the `EXPECTED_BASE`-specific merge below while the origin-relative fetch+merge still runs on its own". The next sentence repeats it: "The adopt path still runs Post-creation catch-up's `git fetch origin {integration-branch}` / `git merge origin/{integration-branch}` unconditionally — that half protects the 'worktree fell behind' direction regardless of how old the adopted worktree is, exactly as this file's own text already states (…)". The second sentence adds two things the first lacks (the literal commands and the "regardless of how old" rationale); everything else is a restatement. The section's own Post-creation catch-up already documents the origin-relative half in full (its "worktree fell behind the integration branch" paragraph), so the Adopt-or-create branch needs one pointer, not two.

## Deliverables
- [ ] Collapse the two sentences into one that keeps every distinct fact: no `EXPECTED_BASE` capture on adopt; the origin-relative `git fetch` / `git merge origin/{integration-branch}` half still runs unconditionally; it runs regardless of the adopted worktree's age; the pointer to Post-creation catch-up's "no `EXPECTED_BASE` to capture" case.
- [ ] Leave every literal `tests/worktree-adopt-or-create-consolidation.test.js` matches in the region between `## Adopt-or-create` and `## Pre-creation reconcile` intact: the detection-command phrase, "Already isolated", "Not isolated", "adopt", `git status --porcelain`, `ExitWorktree(action: "keep")`, `#447`, and the section name.
- [ ] No other paragraph in the file changes.

## Acceptance Criteria
1. The "Already isolated" branch mentions the origin-relative fetch+merge exactly once, with the two commands, the "unconditionally"/age-independence point, and the Post-creation catch-up cross-reference all present.
2. `tests/worktree-adopt-or-create-consolidation.test.js` and every other suite that reads `_shared/worktree-setup.md` pass unmodified; `npm test` is green.
3. Prose-only change: no step, command, or branch condition in the section is altered.

## Technical Approach
One-paragraph edit. Fold the second sentence's two extra facts (commands, age independence) into the first sentence's cross-reference and delete the rest of the second sentence; keep the sentence that follows ("The caller records the actual current branch name …") untouched, since #1751 cites it as the fact source for a sibling doc fix.

### Key Files
- `plugin/skills/_shared/worktree-setup.md` — Adopt-or-create section, "Already isolated" branch; Post-creation catch-up section (read-only cross-reference target).
- `tests/worktree-adopt-or-create-consolidation.test.js` — region-anchored pins.

## Gotchas
- The record's "near line 162" location predates the file's restructuring; the sentences sit inside the Adopt-or-create section's "Already isolated" paragraph today.
- The Post-creation catch-up section's own "worktree fell behind the integration branch" paragraph is the canonical statement; do not move content there, only point at it.
- #1751 (build/worktree-setup.md) and this record touch different files but cite the same paragraph; either order lands cleanly.

## Original request

Whole-branch pre-release review finding (v6.113.0 -> origin/main, d68ffa7d9..7036ce768): pure prose bulk with no added information, in a file whose purpose is consolidation meant to remove duplication elsewhere.


