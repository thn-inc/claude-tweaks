---
record: 2228
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 2228: reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T061711-record-1675

Surface: infra

## Current State

Reconcile's automated hygiene sweep flags this pipeline run directory as `structurally-stuck` (`reason: no-worktree`) for 3 consecutive passes (first observed 2026-09-10T19:55:46.695Z), auto-filing this issue per `bin/lib/reconcile`'s escalation mechanism (#644 Deliverable 2 / #1613's visibility layer).

The stuck directory: `.claude-tweaks/pipelines/2026-09-01T061711-record-1675`. Its `run-state.json` carries only `{pr: {number: 1760, url: ...}, updatedAt}` — **`worktree` was never stamped on this run** (the known #1684 gap: `record-worktree`'s write can be classified foreign and never land, e.g. across a two-Task-call `/flow` run).

Investigation confirms the underlying work already shipped cleanly: issue #1675 is closed, PR #1760 (head `worktree-record-1675`) merged 2026-09-01T06:39:25Z, and `decisions.md` confirms the branch name (`PR-early run lifecycle: pushed worktree-record-1675 to origin` / `opened PR #1760 for worktree-record-1675`) — but the branch itself has since been deleted (standard post-merge cleanup), so `bin/lib/hooks/run-integrity.js`'s `fallbackBranch` (which tries `state.pr.branch`, absent here, then `branchFromDecisions` + `branchExists`) correctly parses `worktree-record-1675` from `decisions.md` but finds no matching `refs/heads/worktree-record-1675` and returns `null`. With no branch derivable at all and `state.worktree` never stamped, `bin/lib/reconcile/archive-merged.js` reports `reason: 'no-worktree'` and reconcile can never resolve this run dir.

`archive-merged.js` already has a `#1962` fallback for exactly this "branch gone, but `run-state.json.pr.number` still names the PR" situation — but it is gated `if (stampedWorktree && state.pr.number)` (~line 1291), and `stampedWorktree` is `null` here (never stamped, not merely torn down), so the fallback never runs for this record. This is a distinct gap from the one in #2226 (which covers the case where `stampedWorktree` *was* set but the probed PR's state came back `MERGED` rather than `CLOSED`) — the same block, two independent reasons it currently falls through to a permanent skip.

## Deliverables

- Widen the `#1962` fallback's outer gate in `archive-merged.js` from `if (stampedWorktree && state.pr.number)` to fire whenever no branch could be derived by any means AND `state.pr.number` exists — regardless of whether `stampedWorktree` was ever set — so a `no-worktree` run dir with a known PR number gets the same by-number probe a `no-branch` run dir already gets.
- Once the code fix lands (this, and/or #2226's MERGED-state recognition — see Gotchas), resolve this specific stuck directory (`2026-09-01T061711-record-1675`) so this issue's own escalation clears.

## Acceptance Criteria

- [ ] `archive-merged.js`'s `#1962` fallback runs its by-number PR probe when `state.pr.number` exists, whether or not `stampedWorktree` is set — the `no-worktree` and `no-branch` skip reasons both reach it.
- [ ] The existing `no-worktree`/`no-branch` skip (no PR number available at all) is unchanged — the widened gate only removes the `stampedWorktree` truthiness requirement, it does not remove the `state.pr.number` requirement.
- [ ] `.claude-tweaks/pipelines/2026-09-01T061711-record-1675` is archived (or otherwise resolved) and no longer reported `structurally-stuck` by reconcile.
- [ ] A test covers the widened gate: `state.worktree` absent, `state.pr.number` set, PR resolvable by number — the fallback fires instead of skipping `no-worktree` outright.

## Technical Approach

In `bin/lib/reconcile/archive-merged.js`, change the `#1962` fallback's guard (around line 1291, currently `if (stampedWorktree && state && state.pr && state.pr.number) {`) to drop the `stampedWorktree` requirement — `if (state && state.pr && state.pr.number) {` — while leaving everything inside the block (the by-number probe, the `CLOSED` check, and the `console-unresolved`/`console-never-rendered` gates) unchanged. The `reason` variable used if the fallback doesn't resolve anything still needs to read correctly as `no-worktree` vs `no-branch` per the existing `stampedWorktree ? 'no-branch' : 'no-worktree'` line — that line is unaffected by this change since it's computed before the widened gate. If #2226's MERGED-state fix lands in the same change, this record resolves fully; if only this widening lands first, this record would still additionally need that fix (the underlying PR is `MERGED`, not `CLOSED`) — see Gotchas.

### Key Files

- `bin/lib/reconcile/archive-merged.js` — widen the `#1962` fallback's outer gate (~line 1291) to drop the `stampedWorktree` requirement
- `bin/lib/hooks/run-integrity.js` — `fallbackBranch`/`branchExists`/`branchFromDecisions` are read-only inputs to this diagnosis; no change needed
- `tests/` (reconcile suite — locate the existing `#1962` fallback test file) — add the widened-gate fixture/case

## Gotchas

- This fix and #2226's ("`byNumber.state === 'MERGED'` never matches the `CLOSED`-only check") touch the exact same `#1962` block in the same function — a builder picking up either should check whether the other has already landed, and likely wants to land both in one PR, since this record's own underlying PR (#1760) is `MERGED`, not `CLOSED`: widening this gate alone is necessary but not sufficient to resolve *this* specific stuck directory without #2226's fix also landing.
- This repo's open queue currently carries several other `reconcile: structurally-stuck` auto-filed issues (e.g. #2226, #2229, #2230, #2231, #2354-#2359) — worth checking, once both fixes land, whether any of them clear automatically on the next reconcile pass. The near-duplicate screen does not flag these against each other by design (reconcile-residue titles are excluded from the title-similarity signal) — this is a manual cross-check, not an automated one.
- `archiveRunDir`'s moves-first, close-last ordering (documented elsewhere in the same file) must be preserved for whatever fix lands here.

## Original request

reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T061711-record-1675

Reconcile has failed `structurally-stuck` on this path for 3 consecutive passes (first observed 2026-09-10T19:55:46.695Z).

**Path:** `/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T061711-record-1675`
**Reason:** `structurally-stuck`
**Last error:** stuck at no-worktree

Filed automatically by `bin/lib/reconcile` — see #644.

<!-- fingerprint: reconcile-residue-1bbf1519 -->
