---
record: 2229
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 2229: reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T044830-record-1678

Surface: infra

## Current State

Reconcile's automated hygiene sweep flags this pipeline run directory as `structurally-stuck` (`reason: no-branch`) for 3 consecutive passes (first observed 2026-09-10T19:55:46.704Z), auto-filing this issue per `bin/lib/reconcile`'s escalation mechanism (#644 Deliverable 2 / #1613's visibility layer).

The stuck directory: `.claude-tweaks/pipelines/2026-09-01T044830-record-1678`. Its `run-state.json` stamps `worktree: .claude/worktrees/record-1678` and `pr: {number: 1759, branch: worktree-record-1678}`.

Investigation confirms the underlying work already shipped cleanly: issue #1678 is closed, PR #1759 merged 2026-09-01T14:31:35Z, and both the worktree and the `worktree-record-1678` branch have since been deleted (standard post-merge cleanup) — there is nothing left to build or resolve; the run directory is legitimately done and should have been archived.

This is the same root cause tracked in #2226 (a sibling `reconcile: structurally-stuck` record, filed against a different directory): `bin/lib/reconcile/archive-merged.js`'s `#1962` no-branch fallback (~line 1291) probes the PR by number via `resolvePrStateByNumber` when the stamped worktree and its branch are both gone, but only archives when `byNumber.state === 'CLOSED'` — a closed-without-merging PR. A PR that was actually merged reports `state: 'MERGED'` (GitHub's 3-value enum: `OPEN`/`CLOSED`/`MERGED`), which this check never matches, so a merged-then-cleaned-up run permanently falls through to the plain `no-branch` skip on every pass — exactly the symptom this issue reports.

## Deliverables

- Fix belongs with #2226, which already deliverables the code change for this exact root cause: extend `archive-merged.js`'s `#1962` fallback to also recognize `byNumber.state === 'MERGED'`, gated by `localHasMerge(root, byNumber.mergeCommit)` (see #2226 for the full deliverable). This record should not duplicate that fix's spec — it names a second, independent instance of the same bug.
- Once #2226's fix lands, resolve this specific stuck directory (`2026-09-01T044830-record-1678`) — either let the next reconcile pass archive it automatically, or archive it manually if reconcile's cadence doesn't reach it in reasonable time — so this issue's own escalation clears.

## Acceptance Criteria

- [ ] `.claude-tweaks/pipelines/2026-09-01T044830-record-1678` is archived (or otherwise resolved) and no longer reported `structurally-stuck` by reconcile.
- [ ] Confirmed this directory's stuck state is resolved by #2226's fix landing (same root cause) rather than needing an independent code change — if investigation at build time finds a *different* mechanism for this specific directory, treat that as new information and re-diagnose rather than assuming #2226's fix covers it.

## Technical Approach

No new code investigation needed here beyond confirming #2226's fix (once it lands) actually resolves this directory too — same `no-branch` + merged-and-deleted-branch pattern, same `run-state.json` shape (`worktree` + `pr.branch` both stamped, both now gone, `pr.number` present and resolves to a `MERGED` PR). See #2226's Technical Approach for the code-level fix.

### Key Files

- `bin/lib/reconcile/archive-merged.js` — same file, same `#1962` fallback block #2226 fixes; no additional change expected here
- (no new Key Files beyond #2226's — this record is a duplicate symptom, not a duplicate fix)

## Gotchas

- This is one of several `reconcile: structurally-stuck` auto-filed issues sharing the same underlying #1962-fallback gap (see also #2226, #2228, #2230, #2231, #2354-#2359) — #2226 carries the actual code fix; #2228 carries a second, independent gap in the same block (the `stampedWorktree` gate excluding the `no-worktree` case). Once both land, re-check every sibling `structurally-stuck` issue rather than resolving each one's directory by hand.
- The near-duplicate screen does not flag these reconcile-residue records against each other by design (title-similarity is deliberately excluded for `reconcile:`-prefixed titles) — this cross-reference is a manual note, not an automated signal.

## Original request

reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T044830-record-1678

Reconcile has failed `structurally-stuck` on this path for 3 consecutive passes (first observed 2026-09-10T19:55:46.704Z).

**Path:** `/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T044830-record-1678`
**Reason:** `structurally-stuck`
**Last error:** stuck at no-branch

Filed automatically by `bin/lib/reconcile` — see #644.

<!-- fingerprint: reconcile-residue-a49452b1 -->
