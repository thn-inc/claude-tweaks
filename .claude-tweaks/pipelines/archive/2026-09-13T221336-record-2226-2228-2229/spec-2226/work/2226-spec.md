---
record: 2226
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 2226: reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T064302-record-1670

Surface: infra

## Current State

Reconcile's automated hygiene sweep flags this pipeline run directory as `structurally-stuck` (`reason: no-branch`) for 3 consecutive passes (first observed 2026-09-10T19:55:46.685Z), auto-filing this issue per `bin/lib/reconcile`'s escalation mechanism (#644 Deliverable 2 / #1613's visibility layer).

The stuck directory: `.claude-tweaks/pipelines/2026-09-01T064302-record-1670`. Its `run-state.json` stamps `worktree: .claude/worktrees/record-1670` and `pr: {number: 1761, branch: worktree-record-1670}`.

Investigation confirms the underlying work already shipped cleanly: issue #1670 is closed, PR #1761 merged 2026-09-01T14:31:51Z, and both the worktree and the `worktree-record-1670` branch have since been deleted (standard post-merge cleanup) — there is nothing left to build or resolve; the run directory is legitimately done and should have been archived.

Root cause: `bin/lib/reconcile/archive-merged.js`'s `#1962` no-branch fallback (~line 1291) probes the PR by number via `resolvePrStateByNumber` when the stamped worktree and its branch are both gone, but only archives when `byNumber.state === 'CLOSED'` — a closed-without-merging PR. A PR that was actually merged reports `state: 'MERGED'` (GitHub's 3-value enum: `OPEN`/`CLOSED`/`MERGED`), which this check never matches, so a merged-then-cleaned-up run permanently falls through to the plain `no-branch` skip on every pass — exactly the symptom this issue reports.

## Deliverables

- Extend `archive-merged.js`'s `#1962` fallback to also recognize `byNumber.state === 'MERGED'`, and — mirroring the general merged-PR archive path's own safety check — verify `localHasMerge(root, byNumber.mergeCommit)` before archiving (never archive ahead of a merge commit the local checkout hasn't actually caught up on).
- Once the code fix lands, resolve this specific stuck directory (`2026-09-01T064302-record-1670`) — either let the next reconcile pass archive it automatically, or archive it manually if reconcile's cadence doesn't reach it in reasonable time — so this issue's own escalation clears.

## Acceptance Criteria

- [ ] `archive-merged.js`'s `#1962` no-branch fallback archives a run dir whose stamped worktree and branch are both gone AND whose PR (resolved by number) is `MERGED`, subject to the same `localHasMerge` gate the general merged-PR path already applies.
- [ ] A PR that is `CLOSED` without merging continues to archive immediately (existing behavior unchanged).
- [ ] A merged PR whose commit is not yet locally reachable is skipped with `local-behind-merge`/`merge-commit-unknown` (mirroring the general path), never archived prematurely.
- [ ] `.claude-tweaks/pipelines/2026-09-01T064302-record-1670` is archived (or otherwise resolved) and no longer reported `structurally-stuck` by reconcile.
- [ ] A test covers the new MERGED branch of the `#1962` fallback (fixture: stamped worktree gone, branch gone, `run-state.json.pr.number` set, `gh pr view` returns `state: MERGED`).

## Technical Approach

Add a `MERGED` branch alongside the existing `CLOSED` check in the `#1962` fallback (`bin/lib/reconcile/archive-merged.js`, the block starting `if (stampedWorktree && state && state.pr && state.pr.number) {`, around line 1291): when `byNumber.state === 'MERGED'`, run the same `localHasMerge(root, byNumber.mergeCommit)` check the general merged-PR path (further down in the same function) already performs, and only archive when it resolves `true`; otherwise skip with `local-behind-merge`/`merge-commit-unknown` (reusing those existing reason strings, not inventing new ones), matching the general path's own handling. The existing `CLOSED` branch, its `console-unresolved`/`console-never-rendered` gates, and the archival call itself (`archiveRunDir`) are unchanged and reusable as-is.

### Key Files

- `bin/lib/reconcile/archive-merged.js` — extend the `#1962` no-branch-fallback block (~line 1291) to also handle a `MERGED` PR state, gated by `localHasMerge`
- `bin/lib/reconcile/pr-state.js` — `resolvePrStateByNumber` already returns `mergeCommit`; no change needed here, just confirms the field is available
- `tests/` (reconcile suite — locate the existing `#1962` no-branch fallback test file) — add the MERGED-state fixture/case

## Gotchas

- This repo's open queue currently carries several other `reconcile: structurally-stuck` auto-filed issues (e.g. #2228, #2229, #2230, #2231, #2354, #2355, #2356, #2357, #2358, #2359) — worth checking, once this fix lands, whether any of them share the identical merged-but-branch-deleted root cause and would clear automatically on the next reconcile pass rather than needing individual manual resolution. The near-duplicate screen does not flag these against each other by design (reconcile-residue titles are excluded from the title-similarity signal specifically so a drain doesn't flag every residue record against every other one) — this is a manual cross-check, not an automated one.
- `archiveRunDir`'s moves-first, close-last ordering (documented elsewhere in the same file) must be preserved for whatever fix lands here — a failure partway through must leave the run dir non-terminal and retryable next pass, not silently lost.

## Original request

reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T064302-record-1670

Reconcile has failed `structurally-stuck` on this path for 3 consecutive passes (first observed 2026-09-10T19:55:46.685Z).

**Path:** `/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-09-01T064302-record-1670`
**Reason:** `structurally-stuck`
**Last error:** stuck at no-branch

Filed automatically by `bin/lib/reconcile` — see #644.

<!-- fingerprint: reconcile-residue-50990638 -->
