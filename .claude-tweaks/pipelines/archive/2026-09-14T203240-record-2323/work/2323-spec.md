---
record: 2323
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 2323: Release bootstrap: detect goreleaser.yaml, package.json release keys, and standard-version as conflicts

Origin: wrap-up ledger gate for #2253 (run ledger row 32)

Defer-reason: genuinely-larger

## Current State

#2253's conflict detection is scope-faithful to the record's enumerated marker list — `.releaserc*`, `release.config.*`, `.changeset/`, `.goreleaser.*`, a foreign `release-please-config.json` — and misses three real-tooling shapes the whole-branch review named: bare `goreleaser.yaml` / `goreleaser.yml` (no leading dot, a supported GoReleaser filename), semantic-release configured under a `release` key inside `package.json`, and `standard-version` (`.versionrc*`). A repo using any of these bootstraps as `fresh` and gets a second engine layered on top — exactly the mixing failure design stance 7 refuses. Run ledger row 32.

## Deliverables

Extend `CONFLICT_MARKERS` in `plugin/bin/lib/init/release-bootstrap.js` with the three shapes (the `package.json` `release` key needs a JSON read, not a name match — add it as a second detection pass after the name scan).

Mirror the additions in `step-21-release.md`'s `conflict` row and add one fixture per shape to `tests/bin-lib/init/release-bootstrap.test.js`.

## Acceptance Criteria

A root `goreleaser.yaml`, a `package.json` carrying a `release` object, and a `.versionrc.json` each yield `conflict` naming the tool; the existing eight fixtures keep passing.

_Filed by `wrap-up` via specShapedBody._


---
Filed by the multi-spec wrap-up of run `2026-09-11T204239-spec-2251-2252-2253-2254-2255-2256-2258` (queue write from spec 2253; source `spec-2253/staged/ledger-record-conflict-marker-gaps.md`; consoleAutoResolve at `autonomy: unattended`).

https://claude.ai/code/session_018rz67jb18j1RLSqhjEdYWH

