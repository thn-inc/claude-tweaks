---
record: 2670
origin: capture
risk: low
size: low
ceremony: fast-lane
grants: []
surface: infra
---
# 2670: browse: resolve or confirm no-equivalent for agent-browser press command

Surface: infra

## Current State
The 5-part playwright-cli-consumer-migration (#2645-#2649) translated `agent-browser` consumers to Playwright CLI, but left one action untranslated: `plugin/agents/qa-agent.md`'s `press` action (line ~120-121) still carries an inline comment marking it explicitly out of scope:

```
<!-- playwright-cli: `press` has no row in playwright-cli-reference.md's Operation vocabulary table — out of scope for #2645 (see its Non-Goals), left untranslated -->
- `press` (no locator — acts at whatever currently has focus) → `agent-browser --session <story-id> press "<value>"` (the story's `value` field supplies the key/combo, e.g. `Enter`, `Control+a`, `Alt+ArrowLeft`; escape per the rule below)
```

`plugin/skills/browse/playwright-cli-reference.md`'s Operation vocabulary table (from line 46) has no `press`/key-press row at all — neither a resolved translation nor a documented negative result. The table already contains negative-result rows for other investigated-and-confirmed-absent capabilities (`batch`, `doctor`, `Capture Web Vitals`), each following the same pattern: `*(investigated during #2645-#2649's migration — no Playwright CLI equivalent found)* — {workaround if any}`.

No sibling record from the #2645-#2649 migration resolved `press`.

## Deliverables
1. Check Playwright CLI's own bundled self-docs (`node_modules/playwright-core/lib/tools/skills/playwright-cli/SKILL.md` and its `references/` sub-docs — the Authority section at the top of `playwright-cli-reference.md` names these as outranking the reference file) for a key-press or key-combo-send command.
2. If found: add a row to `playwright-cli-reference.md`'s Operation vocabulary table translating `agent-browser --session <name> press "<key>"` to the found command, following the table's existing row format (agent-browser column | Playwright CLI column, annotated per-tool-docs/spike-confirmed per the table's own verification convention).
3. If not found: add a negative-result row to the same table, following the exact pattern of the existing `batch`/`doctor`/`vitals` rows — `*(investigated during #2670's follow-up to #2645-#2649's migration — no Playwright CLI equivalent found)*` — plus any workaround (e.g. an `eval` script dispatching a synthetic keyboard event, if one is documented as viable).
4. Update `plugin/agents/qa-agent.md`'s `press` action (line ~120-121) to either use the resolved translation, or replace the inline "out of scope for #2645" comment with a comment pointing at the negative-result row in `playwright-cli-reference.md` and the actual current behavior (still untranslated, with the now-documented reason).

## Acceptance Criteria
- `playwright-cli-reference.md`'s Operation vocabulary table contains a row for key-press, either a resolved Playwright CLI command or a negative-result row matching the existing `batch`/`doctor`/`vitals` row format.
- `plugin/agents/qa-agent.md`'s `press` action comment no longer says "out of scope for #2645" — it either points at a resolved command or cross-references the negative-result row.
- If a translation was found, `qa-agent.md`'s `press` action itself is updated to invoke the Playwright CLI command instead of the agent-browser one.
- If no translation exists, `qa-agent.md`'s `press` action explicitly still uses (or documents the absence of) a working mechanism for QA stories whose `value` field requires a key-press action, so a story author knows what to expect rather than hitting a silent gap.

## Technical Approach
Read `node_modules/playwright-core/lib/tools/skills/playwright-cli/SKILL.md` and its `references/` sub-docs for any keyboard/key-press capability (search for "key", "press", "keyboard"). Cross-reference against `@playwright/cli` 0.1.21 (the version this repo's reference file is pinned to) or whatever version is currently installed — re-verify the pin if it has moved. Follow the exact editing pattern the #2645-#2649 migration already established for this table (see the `batch`/`doctor`/`vitals` rows) so this record's addition reads as part of the same table, not a bolted-on afterthought.

### Key Files
- `plugin/skills/browse/playwright-cli-reference.md` — add the resolved or negative-result `press`/key-press row to the Operation vocabulary table (~line 46 onward)
- `plugin/agents/qa-agent.md` — update the `press` action (~line 120-121) to use the resolved command, or update its comment to reference the negative-result row instead of the stale "out of scope for #2645" note

## Gotchas
- Confirm the installed `@playwright/cli` version still matches the 0.1.21 pin the reference file cites before trusting its self-docs as current — re-verify per the file's own Authority section if the version has moved.
- If a key-press equivalent is found, check whether it operates on a ref (an element) or on "whatever currently has focus" like `agent-browser`'s `press` — the semantics may not translate directly even if a command exists.
- Keep the new row's phrasing consistent with the existing table's verification annotations (`spike-confirmed`, `per tool docs, not spike-tested`, or the negative-result phrasing) rather than inventing new wording.

## Original request

browse: resolve or confirm no-equivalent for agent-browser press command

**Related:** #2645, #2648

Context: 5-part playwright-cli-consumer-migration (#2645-#2649) left qa-agent.md's `press` action untranslated — no sibling record resolved this agent-browser command.

Scope: Check Playwright CLI's self-docs for a key-press equivalent; add to playwright-cli-reference.md if found, or a negative-result row if not, per the migration's convention.

