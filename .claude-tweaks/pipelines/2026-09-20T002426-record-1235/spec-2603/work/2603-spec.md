---
record: 2603
origin: capture
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
---
# 2603: materialize-format.js's REQUIRED_SECTIONS still enforces only three sections, not the four the canonical check requires

Origin: capture

## Current State

`plugin/bin/lib/issues/materialize-format.js`'s `REQUIRED_SECTIONS` constant (line 9) still
lists only three sections — `## Current State`, `## Deliverables`, `## Acceptance Criteria` —
with no `## Release Note`. This means `/claude-tweaks:flow`'s own Materialization hard gate
(and `flow/materialize.md`'s own prose describing that gate) does not enforce the fourth
required section, even though `_shared/work-record.md` (line 276) and
`plugin/bin/lib/compose-record/compose.js`'s `REQUIRED_SECTIONS` (line 11) both require four
sections as of #2580. This is a genuine drift discovered while building #2589 (which fixed
three other stale three-section prose restatements but was explicitly scoped to exclude this
file) — a `ready` record missing only `## Release Note` will pass `/flow`'s materialize-time
hard gate silently, and only get caught later by `compose-subject.js`'s merge-time gate.

## Deliverables

- [ ] Add `'## Release Note'` to `plugin/bin/lib/issues/materialize-format.js`'s
      `REQUIRED_SECTIONS` array (line 9)
- [ ] Update `flow/materialize.md`'s "Materialization hard gate" section prose (currently:
      "the sections `## Current State`, `## Deliverables`, and `## Acceptance Criteria` are
      present and each non-empty") to name all four sections, matching the fix pattern #2589
      already applied to shaping-mode.md/capture/SKILL.md/refine-mode.md
- [ ] Check `tests/bin-lib/issues/materialize-format.test.js` for any fixture relying on the
      three-section gate and update if needed

## Acceptance Criteria

1. `materialize-format.js`'s `REQUIRED_SECTIONS` matches `_shared/work-record.md`'s canonical
   four-section definition
2. A record materialized via `/claude-tweaks:flow` whose only gap is a missing/empty
   `## Release Note` section is stopped by the Materialization hard gate, the same way a
   missing `## Current State` already is
3. `flow/materialize.md`'s hard-gate prose names all four sections

## Release Note

Closed the last mechanical gap in the four-section spec-shaped-body rollout (#2580): /flow's
own materialize-time hard gate now enforces `## Release Note` too.

## Technical Approach

Same class of fix as #2589 (one-clause prose/constant sync to the already-shipped four-section
canonical definition), but in the mechanical JS gate rather than human-facing prose —
`materialize-format.js`'s `REQUIRED_SECTIONS` array is a one-line change; `flow/materialize.md`'s
hard-gate section needs the same prose update #2589 applied elsewhere.

### Key Files

- `plugin/bin/lib/issues/materialize-format.js` — line 9's `REQUIRED_SECTIONS` array
- `plugin/skills/flow/materialize.md` — "Materialization hard gate" section prose

## Gotchas

- Verify current line numbers at build time — discovered 2026-09-18 during #2589/#2592's
  build; other work may have shifted them since
- Check whether any currently-`ready` record in the live backlog would newly fail this
  tightened gate (a record materialized under the old three-section gate that never got a
  Release Note added) — if so, that's a pre-existing gap this fix would surface, not a
  regression it introduces
- `bin/lib/issues/record.js`'s `specShapedBody()` composer (used by `/capture`'s Shaped-body
  branch and every health-sweep filing) also does not compose a `## Release Note` section —
  noticed while filing this very record by hand. Out of this record's own named scope
  (`materialize-format.js` + `materialize.md` only), but worth a human glance when this is
  picked up: it may be the same drift class at a fourth site.

_Filed by `capture` via specShapedBody._

