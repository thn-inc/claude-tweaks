# #1982 — hooks.js hardcodes one bespoke recovery message for archive-merged's audit-untracked reason, generic for ~15 others

Materialized from GitHub issue #1982 (labels: backlog:category-technical, ready, risk:low, size:low, type:task, auto:build, auto:merge, priority:low, ceremony:fast-lane, shaped:headless). An existing open draft PR #2242 (`worktree-record-1982` branch, opened 2026-09-11, build+test checked, review/wrap-up pending) already targeted this issue from a prior run whose claim has since been released/stale-swept (per this run's claim-registry check) — that prior remote branch is now far behind current `main` (unrelated history at the branch-name level: this run's freshly-cut local branch of the same name will not fast-forward-push over it). Re-implementing independently in this run.

## Deliverables

- `archive-merged.js` gains one refusal constructor, `refusal(reason, { hint = null, ...extra } = {})` → `{ ok: false, reason, hint, ...extra }`; every `{ ok: false, reason: … }` literal in the file goes through it.
- The `audit-untracked` recovery text moves verbatim from `hooks.js` into that refusal's `hint`; `move-failed` and the `git-mv-failed*` refusals set `hint` from `lastError` when present; other codes start with `hint: null`.
- `hooks.js`'s `archive-run` branch drops the `reason === 'audit-untracked'` match and prints `claude-tweaks: archival refused — {reason}` followed by ` — {hint}` when `result.hint` is a non-empty string; exit code and `moved:` lines unchanged.
- A conformance test that every `reason: '…'` literal in `archive-merged.js` sits inside a `refusal(` call; unit cases that `refusal` always yields a `hint` key and that `audit-untracked`'s hint names its files.
- `tests/archive-run-verb.test.js` passes unmodified; `tests/bin-lib/reconcile/archive-merged.test.js` deep-equal cases updated to include `hint: null` where whole objects are asserted.

## Acceptance Criteria

1. `hooks.js` contains no comparison against a specific `archive-merged.js` reason string; the `archive-run` branch reads only `ok`, `reason`, `hint`, `movedEntries`.
2. `node bin/hooks.js archive-run` on the untracked-audit fixture prints the same `audit-untracked` sentence as today; every other refusal prints its reason exactly as today, with a hint appended only where set.
3. Every refusal object has a `hint` property; source scan finds no bare `{ ok: false, reason:` literal outside `refusal`.
4. `npm test` is green.

## Gotchas

- The `audit-untracked` sentence is regex-asserted in `tests/archive-run-verb.test.js` — move it character-for-character.
- `trackArchiveResult` consumes `result.reason`/`result.lastError` for residue tracking (see #2330) — adding `hint` must not change what it reads; `lastError` stays its own field, not folded into `hint`.
- Do not print hints for `dryRun` skips (a different consumer).

## Key Files

- `plugin/bin/lib/reconcile/archive-merged.js` — `archiveRunDir`, `archiveOrphanedMint`, `revertWorkMoves`/`revertPlainMoves`, every `ok: false` literal.
- `plugin/bin/hooks.js` — the `archive-run` verb's refusal printing.
- `tests/archive-run-verb.test.js`, `tests/bin-lib/reconcile/archive-merged.test.js`.
