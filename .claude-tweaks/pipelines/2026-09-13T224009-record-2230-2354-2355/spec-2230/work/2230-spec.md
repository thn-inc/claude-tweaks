---
record: 2230
origin: human
risk: low
size: low
ceremony: fast-lane
grants: [build, merge]
surface: infra
---
# 2230: reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-08-26T191436-record-573

Surface: infra

## Current State

Reconcile's automated hygiene sweep flags this pipeline run directory as `structurally-stuck` (`reason: no-worktree`) for 3 consecutive passes (first observed 2026-09-10T19:57:02.528Z), auto-filing this issue per `bin/lib/reconcile`'s escalation mechanism (#644 Deliverable 2 / #1613's visibility layer).

The stuck directory: `.claude-tweaks/pipelines/2026-08-26T191436-record-573`. Its `run-state.json` carries only `{pr: {number: 1509, url: ...}, updatedAt}` — `worktree` was never stamped (the known #1684 gap).

Investigation confirms the underlying record is already resolved: issue #573 is closed, and PR #1509 (head `worktree-dispatch-record-573`) is `CLOSED` without merging (`mergedAt: null`) — this is exactly the case `bin/lib/reconcile/archive-merged.js`'s `#1962` fallback was built to recognize (`byNumber.state === 'CLOSED'` → archive immediately, no merge-commit wait needed). Root cause: the fallback was gated `if (stampedWorktree && state.pr.number)`, and `stampedWorktree` was `null` (never stamped) — so a run the existing fallback logic would resolve on the very next pass never even reached that logic.

## Deliverables

This is the same gap tracked in the sibling `reconcile: structurally-stuck` records #2226/#2228/#2229 — already fixed and merged to `main` in PR #2386 (commit `4c3848121a7af20719a0d04f9462876be476c006`): the `#1962` fallback's outer gate now fires whenever `state.pr.number` exists, regardless of whether `stampedWorktree` was ever set, and recognizes both `CLOSED` and `MERGED` PR states.

- Confirmed by direct verification (this build): `gh pr view 1509` reports `state: CLOSED`, `mergedAt: null` — exactly the case the now-merged fix's `CLOSED` branch resolves, and the `stampedWorktree` requirement it used to need has already been dropped by #2386.
- Confirmed the merged fix's existing regression test (`tests/bin-lib/reconcile/archive-merged.test.js`, "a run dir with no worktree stamp at all (never set) still reaches the #1962 by-number fallback and archives once its closed PR is confirmed") exercises this exact shape: no `worktree` key at all, `pr.number` set, `gh pr view` returns `CLOSED`/`mergedAt: null` → archives.
- No additional code change is needed in `archive-merged.js` for this record — the fix already ships on `main`.
- Once reconcile's next pass runs against the main checkout, `.claude-tweaks/pipelines/2026-08-26T191436-record-573` archives automatically; if reconcile's cadence doesn't reach it in reasonable time, a human (or `/tidy`) can archive it manually via `node "${CLAUDE_PLUGIN_ROOT}/bin/hooks.js" archive-run --run ".claude-tweaks/pipelines/2026-08-26T191436-record-573"`.

## Acceptance Criteria

- [x] Confirmed this directory's stuck state is resolved by #2228's fix (shipped as part of #2386) landing alone (same `no-worktree` + `stampedWorktree`-gate gap, PR genuinely `CLOSED`) — verified via `gh pr view 1509` (`CLOSED`, `mergedAt: null`) and the existing no-worktree/CLOSED regression test in `archive-merged.test.js`.
- [ ] `.claude-tweaks/pipelines/2026-08-26T191436-record-573` is archived (or otherwise resolved) and no longer reported `structurally-stuck` by reconcile — resolves on the next reconcile pass against the main checkout, since the code fix is already live on `main`.

## Technical Approach

No new code change: PR #2386 (merged to `main` prior to this build) already widened `archive-merged.js`'s `#1962` fallback gate to drop the `stampedWorktree` requirement and to recognize both `CLOSED` and `MERGED` PR states. This record's own investigation (PR #1509 state, sibling-fix cross-check) confirms the shipped fix covers this exact directory; the stuck directory itself resolves via reconcile's normal sweep cadence, not a build-time code edit.

### Key Files

- `plugin/bin/lib/reconcile/archive-merged.js` — already fixed by #2386; no additional change
- `tests/bin-lib/reconcile/archive-merged.test.js` — already covers this exact scenario (no-worktree + CLOSED, added by #2386)

## Gotchas

- This is one of several `reconcile: structurally-stuck` auto-filed issues sharing the `#1962`-fallback gap family (see also #2226, #2228, #2229, #2231, #2354-#2359). #2226/#2228/#2229's fix landed as PR #2386 before this record's build ran — confirmed via `git log`/`gh pr view 2386` at build time rather than assumed.
- The near-duplicate screen does not flag these reconcile-residue records against each other by design (title-similarity is deliberately excluded for `reconcile:`-prefixed titles) — this cross-reference is a manual note, not an automated signal.

## Original request

reconcile: structurally-stuck stuck on /Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-08-26T191436-record-573

Reconcile has failed `structurally-stuck` on this path for 3 consecutive passes (first observed 2026-09-10T19:57:02.528Z).

**Path:** `/Users/thomasholknielsen/Code Workspaces/claude-tweaks/.claude-tweaks/pipelines/2026-08-26T191436-record-573`
**Reason:** `structurally-stuck`
**Last error:** stuck at no-worktree

Filed automatically by `bin/lib/reconcile` — see #644.

<!-- fingerprint: reconcile-residue-7f8a51cb -->
