# Staged: Delete merged remote branch — origin/worktree-record-1936-2008-2265-subagentstop

**Finding:** `origin/worktree-record-1936-2008-2265-subagentstop` is already merged into `main` but not yet deleted — reconcile's own `remote-prune` check has not converged it (mirror-ff reported "not fast-forwarded (dirty)" earlier this run, so background convergence may be behind).

**Proposed:** Delete the merged remote branch — an outward-facing push, staged for approval per the auto-mode contract's reversibility floor (never auto-applied at any tidy tier).

**Commands:**
```
git push origin --delete worktree-record-1936-2008-2265-subagentstop
```
