# Open Items — Flow run: #2050 (Task-tool subagents can't be redirected to a pre-existing worktree via EnterWorktree(path=) in this harness)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Deliverable 1's confirm-harness-specificity call, and Acceptance Criterion 1's "avoids the failed pattern by construction, or fails with a clear, actionable message" claim, are unverified against a real two-call Task dispatch that hits this exact shape (a second call joining a first call's worktree via `EnterWorktree(path=)`) (reason-not-auto: live-verification — the only way to confirm or disprove this is a real Task/Agent dispatch creating and joining worktrees against shared harness state; this build was itself policy-restricted from making any Task/Agent dispatch of its own, per this firing's own dispatch prompt) | acknowledged | Auto-acknowledged at wrap-up (`opsAckAutoAcknowledge`, autonomy=unattended) — staged as a born-ready backlog record, `staged/ledger-record-il-155-live-verify.md` (Defer-reason: blocked-external), resolved at this run's Review Console Queue writes section. |
| 2 | wrap-up | pr — PR #2293 — `gh pr list --state open --json number,title,headRefName --limit 100` — open, head worktree-record-2050-enterworktree-path-subagent (this work) | resolved | PR #2293 merged 2026-09-12T13:29:55Z (superseding the prior residue-sweep finding, which had left it open pending a human merge decision this headless dispatch had no channel to obtain). No further action needed on this row. |
| 3 | wrap-up | suite — test suite exit 1 — `not ok 6414 - the installed CLI matches the pinned version` (`tests/impeccable-cli-contract.test.js`) | resolved | Backlog #2277 (the tracked local-machine installed-CLI-version drift behind this pre-existing, unrelated failure) closed 2026-09-12T15:36:33Z via PR #2309 — superseding the prior residue-sweep finding, which had left this row open pending a human's `accepted` close using the proof gathered here. No further action needed on this row. |
