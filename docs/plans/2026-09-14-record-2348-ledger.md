# Open Items — worktree-setup: adopted worktree can silently resolve to stale main-checkout node_modules

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | High/Error handling: `_shared/worktree-setup.md`:90-93 — Step 3's staleness-check bash fence uses `$LOCKFILE`, but it was assigned in Step 1's separate fenced block; if the two fences run as separate Bash invocations (the natural reading of two numbered steps), `$LOCKFILE` arrives empty in Step 3 and `[ "" -nt node_modules ]` silently evaluates FRESH every time, making the staleness check a permanent no-op. Confirmed empirically (bash script reproducing the exact construct). | open | Staged: `staged/review-2348-1.patch` |
| 2 | review | Medium/Error handling: `_shared/worktree-setup.md`:78 — `ls package-lock.json yarn.lock pnpm-lock.yaml 2>/dev/null \| head -1` sorts its output alphabetically regardless of argument order, so when more than one lockfile is present (leftover npm lockfile after a yarn/pnpm migration, or a monorepo mixing managers) the wrong lockfile is silently selected — confirmed empirically. Reproduced by both dispatched lens agents (3c-a and 3c-b) independently, from different concrete scenarios. | open | Staged: `staged/review-2348-1.patch` |
| 3 | review | Medium/Error handling: `_shared/worktree-setup.md`:94-97 — prose claims the staleness comparison "degrades to FRESH" on failure (permissions, broken symlink), but bash's `-nt` actually evaluates true (STALE) when the right-hand operand doesn't exist/resolve — the opposite of the documented safety direction. Confirmed empirically (bash script reproducing the exact construct). | open | Staged: `staged/review-2348-1.patch` |
| 4 | review | Low/Convention: `_shared/worktree-setup.md`:99 — cross-reference to `dev-url-detection.md` omits the `_shared/` prefix this file uses for its other same-directory citations (`_shared/git-discipline.md`, `_shared/worktree-base-ref.md`). | fixed | Auto-applied — added `_shared/` prefix. |
| 5 | review | Medium/Test quality: no `node --test` conformance test pins the new "Dependency freshness check (adopt path only)" section, despite this repo's established pattern of pinning load-bearing procedural skill-file sections (e.g. `tests/worktree-adopt-or-create-consolidation.test.js`). Reproduced by both dispatched lens agents (3f-a and 3f-b) independently. | fixed | Auto-applied — added `tests/worktree-dependency-freshness-check.test.js`. |
