# Open Items — console-resolve.js's readMembers fallback misses multi-spec parent run dirs (#2028)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | All deliverables/acceptance criteria in work/2028-spec.md already implemented and tested (PR #2234, branch worktree-record-2028, merged; never closed the issue since that merge carried no closing keyword) — no code changes made this run | observation | Verified byte-for-byte against pack.js/resolve.js and the (#2028)-tagged tests in tests/bin-lib/{console/resolve,wrap-up/pack}.test.js; routed to Wrap-Up Review Console's staged-close surface for the actual close decision |
| 2 | test | `tests/reconcile.test.js:1491` (#872 concurrency timing assertion) failed once under the full 127s/8988-test suite (463ms vs. expected ~200ms window) — unrelated to this run's diff | observation | Isolated re-run (`node --test tests/reconcile.test.js`) passed 103/103, same test at 323ms — adjudicated as flake (machine load), verdict unchanged (TEST_PASSED=true). Flaky-allowlist proposal staged: staged/flaky-allowlist-tests-reconcile-test-js.md |
| 3 | wrap-up (reflect, Near-misses) | `tests/hooks-git-exec.test.js:190,204` (`timeoutMs: 1` races a real `git` subprocess) — 1/1 fail in the full suite, 2/3 fail on isolated re-run, 1/3 clean pass (18/18) — same machine-load-timing-race shape as row 2, unrelated to this run's diff | deferred | Pattern observation staged for the Review Console: staged/reflect-1.md (flaky-allowlist addition alongside row 2's) |
