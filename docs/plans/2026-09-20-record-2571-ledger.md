# Open Items — check-pr-bookkeeping.js's Step 0 gate is a silent no-op for multi-spec /flow runs

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Deliverables 3/4 of #2571 targeted `check-pr-bookkeeping.js`'s own call site and a CLI-level regression test under `tests/bin-lib/pr-bookkeeping/`. Neither exists on `main` — both live only on the still-open, unmerged #2472 (PR #2570). This build instead fixed the shared root-cause helper both layers call (`hasMaterializeCommit` in `plugin/bin/lib/hooks/pre-tool-use.js`), verified via the existing consumer (`checkBookkeepingStampsGate`) with new regression tests in `tests/hooks-bookkeeping-stamps-gate.test.js`. Once #2472 merges, `checkPrBookkeepingPrecondition`/`check-pr-bookkeeping.js` inherit this fix for free (same shared helper) — but the AC's own CLI-level end-to-end regression test (mirroring `tests/bin-lib/pr-bookkeeping/`'s fixtures) still needs to be added at that point, since that module doesn't exist yet to test. Defer-reason: blocked-dependency (fix requires functionality — `check-pr-bookkeeping.js` — not yet built/merged). | deferred | → backlog. Re-checked #2472/PR #2570 at resolve time — still OPEN, unmerged. Staged `staged/ledger-record-2571-pr-bookkeeping-test.md` via ledger Phase 2 auto-narrowing (`ledgerNarrowing`, autonomy=unattended clears `blocked-dependency`'s floor). Resolves at the Wrap-Up Review Console's Queue writes section. |
