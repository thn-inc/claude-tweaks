# Open Items — Playwright CLI consumer migration (parts 3a-3e, #2645-#2649)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Pre-flight sweep (baseline, before any spec's edits): `tests/impeccable-plugin-contract.test.js` — 3 failures (`the installed plugin matches the pinned version`, `gatherSignals() executes cleanly...`, `the CLI entrypoint accepts no flags`). Root cause: installed Impeccable plugin versions are 3.0.6/4.2.0/4.2.2/4.3.1, but the test pins 4.0.2, which is not installed. Unrelated to any of #2645-#2649's key files. | accepted | Pre-existing, proven by the baseline `npm test` run captured before any of the 5 specs edited a single file — confirmed identical (same 3 subtests, same error) across all 5 post-build test runs. User confirmed via Review Console. |
| 2 | build | #2645's build left 4 `agent-browser` command lines untranslated in `plugin/agents/qa-agent.md` (`set viewport`, `auth login`, `auth save`, `press`) — a decomposition gap (no record's Key Files claims ownership of `qa-agent.md`'s residual lines once siblings resolve the underlying gaps). `set viewport`'s fix-exhaustion resolved during the resolve gate: touched up to `resize <w> <h>` now that #2647 landed it (small, safe, `npm test` re-confirmed clean). `auth login`/`auth save` need no further action (confirmed no equivalent exists — `#2646`). `press` remains genuinely unresolved with no sibling record to reuse. | deferred | `press` gap filed as a follow-up backlog record: #2670. |
| 3 | build | #2647's build surfaced two capability losses in `plugin/skills/visual-review/*` that go beyond mechanical CLI-syntax translation: (1) **overlay-numbering convention** — Playwright CLI's `screenshot` has no `--annotate` bounding-box/numbered-marker overlay (also true for #2645/#2648's screenshot translations), but visual-review's prose still references "overlay numbers" ~25+ times across its 5 files describing how findings cite elements; #2647 added one capability-gap note but did not redesign the downstream element-referencing convention (a real design decision, not a rename). (2) **Web Vitals capture** — no Playwright CLI command surfaces LCP/CLS/INP/TTFB/FCP; `browser-review.md`'s own contract calls vitals "a first-class finding category, never skip." #2647 flagged both prominently and stopped short of fabricating a replacement. | deferred | Filed as a follow-up backlog record: #2671. |
| 4 | review | #2648's dispatched build left `qa-prompts.md`'s two dispatch-prompt templates (Structured + Legacy) using stale `find <locator> <value> <action>`/`snapshot -i -c`/`screenshot --annotate` syntax in the per-step action instructions — these lines never literally said "agent-browser" so the build's grep-based self-check missed them, but they're the exact syntax #2645 already established doesn't work under Playwright CLI (`find` is read-only/text-only, no `-i -c`, no `--annotate`). Since this template is the literal prompt handed to the qa-agent subagent #2645 migrated, the mismatch would have broken every QA dispatch. Fixed directly during review (rewrote both templates' action/verify/screenshot steps to snapshot+ref-based click/fill/check/hover and `screenshot --filename=`, matching #2645's pattern exactly); `npm test` re-confirmed clean after the fix. | fixed | Corrected in review before commit (part of #2648's commit) — see commit message. |
| 5 | review | Whole-repo sweep during #2648's review found `plugin/skills/visual-review/discover-mode.md` (line 111) still describes a `batch` invocation bundling `open`/`snapshot -i -c`/annotated `screenshot`/`vitals` — the identical stale-syntax class as item 4, and now-broken under Playwright CLI (`batch`/`-i -c`/`--annotate`/`vitals` are all either gone or resolved differently per `playwright-cli-reference.md`). This file is explicitly out of scope for all 5 records in this migration — #2647's own Non-Goals confirmed "0 agent-browser hits" via literal grep (true — the word never appears there) but that check doesn't catch non-prefixed old-syntax command references, the same false-negative class item 4 hit. No record's Key Files claims this file. Recommend: file a follow-up backlog record via `/claude-tweaks:capture` to migrate `discover-mode.md`'s one `batch` invocation to Playwright CLI syntax once its own gaps (`batch`→no-equivalent per #2646/#2647, `vitals`→no-equivalent per #2647) are simply reused — small, mechanical, but needs its own record since nothing here owns it. | deferred | Filed as a follow-up backlog record: #2672. |
