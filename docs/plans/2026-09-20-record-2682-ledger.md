# Open Items — #2682 (retire agent-browser-reference.md)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Pre-existing `npm test` failures: 3 in `tests/impeccable-plugin-contract.test.js` (installed impeccable plugin version mismatch vs the 4.0.2 pin), unrelated to this run's diff | accepted | Tracked at #2531. Same 3 failures reproduced identically in isolation, consistent with the #2670/#2681 run's own acceptance of the same issue. |
| 2 | test | One additional failure on the full-suite run (`allocate: skips a block with a bound port, moves to the next candidate`, `tests/bin-lib/ports/registry.test.js`) not present on prior runs | accepted | Confirmed flaky, not a regression: passes clean (10/10) in isolation (`node --test tests/bin-lib/ports/registry.test.js`). A port-binding race under concurrent sibling sessions on this shared machine, per CLAUDE.md's documented `npm test` flake-tolerance note — not caused by this run's diff (doc/config-only changes). |
| 3 | review | Build agent's initial `grep -rn agent-browser` sweep missed two live (not historical) behavioral claims: `plugin/skills/flow/SKILL.md` (visual-review browser-detection step description) and `docs/journeys/accept-built-work-via-demo.md` (URL-surface validation description) both still said "agent-browser" when the actual consumers (`/visual-review`, `/demo`) already run on playwright-cli. In #2682's own AC2 scope (a live invocation, not comparison prose) — fixed directly, not deferred. | fixed | Commit `941e52ba9`. Independently re-verified via `grep -rn agent-browser plugin/skills/flow/SKILL.md docs/journeys/accept-built-work-via-demo.md` returning no match after the fix. |
| 4 | build | A sizeable, separate cluster of stale documentation describing `/init`'s detection of `agent-browser` as the current browser-backend mechanism (it isn't — playwright-cli has been the actual mechanism since #2645-#2649): `plugin/skills/init/SKILL.md`, `bootstrap-steps.md`, `summary-templates.md`, `update-mode.md`; `plugin/skills/routine/guided-environment-creation.md`; `plugin/skills/journey-health/routine-template.yml`; `docs/getting-started.md`; `docs/skill-graph.md` (4 rows). Additionally, **`docs/donts.md:37`'s own Don't rule is now factually false**: "`agent-browser` is the only backend working in both interactive sessions and hosted Routines" — this rule should be corrected or retired now that agent-browser is fully removed, not merely left stale. None of these files are in #2682's own declared Key Files (`agent-browser-reference.md`, `step-14-cloud-routine-parity.md`, `scripts/claude-cloud-setup.sh`, `CLAUDE.md`) — genuinely separate scope, not introduced by this build. | deferred | Staged as a new backlog record proposal for Review Console approval — see `staged/backlog-1.md`. |
