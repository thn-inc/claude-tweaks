# Open Items — residue-sweep's documented --own-pr carve-out (#2462)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #2462's premise is already satisfied at base — `bin/residue.js`/`forge.js`/`pack.js` already carry the full `--own-pr` carve-out (commit `bc1754dac`, landed ~1h after this issue was filed) and all relevant tests already pass. No code change made. Staged close proposal: `staged/premise-satisfied-2462.md`. | open | — |
| 2 | test | `npm test` (unmodified checkout, no files touched by this run) reports 9 pre-existing failures unrelated to #2462's scope, isolated by file and confirmed independent of this run's changes: `tests/bin-lib/hooks/reconcile-background.test.js`, `tests/bin-lib/visual-decide/cli.test.js`, `tests/console-execute.test.js`, `tests/impeccable-plugin-contract.test.js` (x3, cascading from an installed-vs-pinned Impeccable plugin version mismatch: 4.3.1 installed vs 4.0.2 pinned), `tests/needs-worklist-rule-cross-consumer.test.js`, `tests/session-limit-degrade-conformance.test.js`, `tests/untrusted-record-content-conformance.test.js` (base-SHA-ancestor preconditions failing against this worktree's git history). None of these files, or any file they exercise, appear in this run's diff (empty diff — no code change). Full report: `.git/worktrees/dispatch-record-2462/claude-tweaks-verify/report.json`. | open | — |
