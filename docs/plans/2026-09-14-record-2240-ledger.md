# Open Items — #2240: parseRepo host-widening for GitHub Enterprise

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | AC #4 unmet: `--hostname` threading missing for 6 gh-invoking `parseRepo` callers audited in the spec's Key Files — `link-records.js` (`lib/issues/link.js`'s `resolveDatabaseIds`/`linkSubIssues`/`linkBlockedBy`, `gh api graphql` + `gh api -X POST`), `file-feedback.js` (`lib/feedback/file-feedback.js`'s `findDuplicate`/`fileDraft`/`readBack`, `gh issue list/create/view` — repo built as bare `owner/repo` discarding `repoSpec.host`), `backlog-grant-gate.js` (`resolveSubIssueNumbers`, `gh api --paginate repos/{owner}/{repo}/...`), `resolve-blockers.js` + `resolve-linked-prs.js` (shared `lib/number-list-cli.js`, destructures `{owner, repo}` only), `repair-claim.js` (owner/repo passed to its claim-store call without host). Fix: thread `host` through each and use `repoSlug()`/`--hostname` per the pattern already applied in `apply-refine-labels.js`/`materialize.js`/`compose-subject.js`/`release-claim.js`/`fetch-sub-issues.js`; add a regression test per caller (fake/injected `gh` runner) exercising a non-`github.com` host, mirroring `tests/fetch-sub-issues-cli.test.js`'s new `#2240` test. | resolved | All 6 named callers fixed (`link.js`/`link-records.js`, `file-feedback.js` now uses `repoSlug()`, `backlog-grant-gate.js`'s REST retry, `number-list-cli.js` now threads `host` into `fetch()` for `resolve-blockers.js`/`resolve-linked-prs.js`, `repair-claim.js`/`repair.js` via a new `ghHost` param distinct from the existing machine-hostname `host`). Widened during this pass beyond the 6 named callers, since the same review-audit gap existed in code these 6 share or that the same Key-Files list covers: `release-claim.js`'s own primary release path (`release.js`'s `readClaimBlob`/`writeTombstone`/`postReleaseComment`/`removeLabel` all previously built a bare `owner/repo`, despite the prior build step's now-incorrect claim that this file was already fixed), `native-dependencies.js`'s `fetchNativeDependencies` and `linked-prs.js`'s `fetchLinkedPRs` (the graphql calls `resolve-blockers.js`/`resolve-linked-prs.js` actually make), and `preflight-records.js`'s own native-mode call (same shared function, now consistent). 24 new regression tests added across 10 test files; full `npm test` green (8847/8847). | Fixed by the #2240 retry build (this run) |
