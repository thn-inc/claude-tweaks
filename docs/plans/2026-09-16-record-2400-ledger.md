# Open Items — claim-targets.js: unverified-claim leak still present under --keep-going (residual of #2329)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Record #2400's Current State assumed `#2329`/PR #2399 already landed `abort()`-side release-on-unverified handling. On this branch's `main` (HEAD `16b24b455`), PR #2399 is still open/unmerged (verified via `mcp__github__issue_read`), so `abort()` and `stopOrSkip`'s `--keep-going` branch already behave symmetrically today — neither releases an unverified target's own write. `plugin/skills/_shared/issue-claims.md`'s Post-write verification section (lines 284-294) already documents and justifies this (a write this run cannot confirm might still be its own valid, slow-to-replicate claim; blind release risks tombstoning a live claim). Tests `(m)`/`(m2)`/`(m3)`/`(m4)` in `tests/bin-lib/claim-targets/claim-targets.test.js` already pin this exact behavior and pass unmodified. Record #2400's own Deliverables/Acceptance Criteria ("...or the doc explicitly states and justifies why it does not") therefore already appear satisfied by pre-existing code and docs. This confirms and re-derives the same finding an earlier run recorded in issue comment https://github.com/thomasholknielsen/claude-tweaks/issues/2400#issuecomment-5691416399 (whose own merge was reverted for a process/authorization reason, not a content reason). No code diff was made this run. Review (this call) independently re-derived this same finding directly from `claim-targets.js` and the live test run — see `decisions.md`'s `SCANNED` review entry. | deferred → backlog | Defer-reason: needs-human-decision — whether #2400 closes as already-satisfied (no diff) or gets fixed anyway is a product/scope call only a human can make. `ledgerNarrowing` (autonomy=unattended) auto-routed to backlog; staged at `staged/ledger-record-2400-disposition.md`, auto-filed via `queueWriteAutoFile` — see `decisions.md`. |
| 2 | test | Full `npm test` (this run, this worktree, HEAD `f71885079`): 9107 pass / 9 fail / 24 skip. This build made zero source-code changes (only added the materialized spec + this ledger file), and `tests/bin-lib/claim-targets/claim-targets.test.js` (the file record #2400 concerns) is 37/37 green in isolation. The 9 failures are all pre-existing/environmental, unrelated to #2400's scope: `tests/impeccable-plugin-contract.test.js` (3 failures) — installed Impeccable plugin is 4.3.1, pinned fixture expects 4.0.2, a local-install mismatch; `tests/needs-worklist-rule-cross-consumer.test.js`, `tests/session-limit-degrade-conformance.test.js`, `tests/untrusted-record-content-conformance.test.js` (1 each) — each asserts a pinned historical commit sha is an ancestor of HEAD, which fails because this checkout is a shallow clone (`git rev-parse --is-shallow-repository` -> true, 51 commits of history) missing that older history; `tests/bin-lib/hooks/reconcile-background.test.js` (1, reproduces in isolation too) and `tests/bin-lib/visual-decide/cli.test.js` / `tests/console-execute.test.js` (1 each) — none touch `claim-targets.js`/`issue-claims.md` and cannot be caused by a diff that added no source changes. | accepted | Pre-existing/environmental (shallow clone + local plugin-version drift), not caused by this build's zero-source-diff change; verified via isolated re-run of the affected suite (claim-targets: 37/37) and inspection of each failure's own assertion |
