# Open Items — #2605: skill-prose-conformance-tests Anti-Patterns trim

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | wrap-up | Case-3 adoption backfilled `config.yml`/materialize/worktree-registration/PR-early lifecycle for this run — the first call's `decisions.md` entry reads as a normal completion, not a crash, which doesn't obviously match case-3's "prior call ended before completing" framing. Worth a look at whether dispatch's two-call `build,test` handoff reliably runs its own `/flow` Step 3 + materialize.md. | deferred | Staged as a reflect observation — `staged/reflect-1.md` — surfaced at the Review Console's Skill updates section |
