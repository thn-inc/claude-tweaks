# Open Items — Build group: #1739, #2336

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #1739 ("captureDebugPayload is permanent scaffolding") — all Deliverables already satisfied at this worktree's base commit. `grep -rn "captureDebugPayload\|CT_HOOKS_DEBUG_CAPTURE" plugin/ tests/` returns no results; the function, its call site, and the three tests were already removed by PR #2435 (merged 2026-09-14, `refs #1739` — never closed the issue). No code diff produced this run; only the materialized spec file was committed. | observation | Already shipped via PR #2435 (`0ca7a69`), an ancestor of this worktree's HEAD. Issue left open because that PR used `refs`, not `closes`, per convention — closing is a wrap-up/review decision, not this build step's. |
| 2 | build | #2336 ("Dangling citations from #2019's pipeline-run-dir.md split") — both Deliverables already satisfied at this worktree's base commit. `research/verify-mode.md` lines ~124/183 and `_shared/pipeline-run-dir.md` lines ~31-34 already cite `run-dir-resolution.md`'s steps 4/2 rather than the stale "above"/allowlist pointers the issue describes. No code diff produced this run; only the materialized spec file was committed. | observation | Already shipped via PR #2435 (`0ca7a69`), an ancestor of this worktree's HEAD. Issue left open because that PR used `refs`, not `closes`. |
| 3 | test | Pre-flight Verify Sweep (`npm test`, full suite) found 30 pre-existing failures on this worktree's unmodified base — unrelated to #1739/#2336 (neither spec touches any of the failing files). Isolated re-runs of two of them (`tests/shaping-mode-needs-removal.test.js`, `tests/tidy-needs-worklist-rule.test.js`) show a `git show <sha>:<path>` "go-red control" pin failing with `fatal: path ... exists on disk, but not in <sha>` — a git-history/shallow-clone shape, not a content regression. Full raw output: `/tmp/npm-test-baseline.log` (30 `not ok` lines, ids 1926, 5225, 5880, 6015-6031, 7229-7234, 7567-7572, 8390-8393, 8940-8945, plus `tests/shaping-mode-needs-removal.test.js` and `tests/tidy-needs-worklist-rule.test.js` as whole-file failures). Review independently re-ran `npm test` in full a second time (`/tmp/npm-test-review-verify.log`) — byte-identical 9183/9129/30-fail/24-skip, same 30 ids — confirming pre-existing and reproducible, not flake. Triaged into 4 distinct clusters (recurrence of #2421's pinned-SHA go-red class; #1208-AC content-assertion drift; 3 unrelated mechanical failures). | deferred | Filed #2597 (backlog, `by:capture`+`type:bug`, spec-shaped body with the 4-cluster triage and both raw logs) — out of scope for #1739/#2336's surgical-changes discipline. Trigger: next session picking up #2597. |
