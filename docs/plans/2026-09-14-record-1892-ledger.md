# Open Items — 1892: bin/lib/reconcile archive-merged.js split-state ENOTEMPTY

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | Every deliverable in this spec (twin-conflict detection, `archived-pending-tracked-move` classification, `residueFailures` pruning, closed-record reopen-on-dedup) is already implemented on `main` and shipped in v6.122.0 via PR #2287 (commits `cde8e4e29`, `6b4685bed`, `0e11fe809` — all confirmed ancestors of this worktree's HEAD, independently re-verified via `git merge-base --is-ancestor`). Recommend: close #1892 referencing PR #2287 rather than merging this PR as new work. Defer-reason: needs-human-decision — #1892 carries no `auto:merge` grant, so this pipeline cannot close or merge autonomously. | deferred | Posted as a PR comment on #2447 (https://github.com/thomasholknielsen/claude-tweaks/pull/2447#issuecomment-5666497944) summarizing the independently-re-verified evidence and the close-vs-merge recommendation; the decision itself is left for the human reviewing the PR (outcome: pending-review, no `auto:merge` grant on #1892). |
| 2 | review | Dispatch's `auto:build` queue selected #1892 for this build despite a strong mention + title-similarity signal from merged PR #2287 that #1984's shipped-candidate exclusion was designed to catch — worth investigating why. Defer-reason: pre-existing-outside-diff — the relevant code (`bin/lib/issues/shipped-candidate.js`, `dispatch/queue-pull-script.md`) is untouched by this record's own diff. | deferred | Filed thomasholknielsen/claude-tweaks#2449 (→ backlog) with the reproduction evidence (title/commit-message/merge-date) and a fixture-based acceptance criterion. |
