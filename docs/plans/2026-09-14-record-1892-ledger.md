# Open Items — 1892: bin/lib/reconcile archive-merged.js split-state ENOTEMPTY

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | Every deliverable in this spec (twin-conflict detection, `archived-pending-tracked-move` classification, `residueFailures` pruning, closed-record reopen-on-dedup) is already implemented on `main` and shipped in v6.122.0 via PR #2287 (commits `cde8e4e29`, `6b4685bed`, `0e11fe809` — all confirmed ancestors of this worktree's HEAD). The `claims-registry` tombstone for issue-1892 independently confirms `"reason": "merged: reconciled from PR #2287"`. `npm test` (8818/8818) passes unmodified — no code changes needed. This PR (#2447) carries only the materialized spec header. Recommend: close #1892 referencing PR #2287 rather than merging this PR as new work; consider why dispatch's #1984 shipped-candidate exclusion did not filter this record out of the `auto:build` queue. | open | — |
