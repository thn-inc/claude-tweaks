# Open Items — Flaky allowlist fixes: #2500 (hooks-git-exec.test.js), #2499 (reconcile.test.js)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | #2500's spec Technical Approach asked to check whether `runGitAsync`'s third Promise.all shape-test call (`tests/hooks-git-exec.test.js:175`, real subprocess + `timeoutMs: 1`) shared the same wall-clock race as the two rewritten tests; the build's commit left it untouched with no documented determination | fixed | Verified it doesn't race meaningfully — the loop only asserts result shape (stdout/failure/stderr keys), never which failure kind (TIMEOUT vs git-error) wins, so no rewrite needed. Added a documenting comment at the call site — no isolated re-run regression (18/18 clean). |
