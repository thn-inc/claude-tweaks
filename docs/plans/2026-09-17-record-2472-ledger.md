# Open Items — pr-first run reached wrap-up with no PR/run-state and no logged degrade trace (#2472)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review/hindsight | [skill: pre-tool-use-gate-exemptions] `pre-tool-use-gate-exemptions` skill's fail-closed-on-unprovable discipline is written for `pre-tool-use.js`'s own deny gates; `plugin/bin/lib/pr-bookkeeping/precondition.js` (#2472) reuses its `isLinkedWorktree`/`indeterminate` scoping pattern but deliberately inverts the posture to fail-open (a benefit-of-the-doubt precondition check, not a deny gate) — worth folding in as a documented sibling pattern next time that skill is touched. | observation | Confirmed skill-worthy (hindsight eval 5) — tagged for wrap-up's Skills curation row; no action now, per next-touch timing already stated |
| 2 | build/docs | docs/hooks.md may need a cross-reference note — it documents `checkBookkeepingStampsGate`'s internal helpers (`hasMaterializeCommit`, `hasLoggedPrDegrade`, `resolveRunPinnedIntegrationModel`) in depth but has no "other consumers" section; `plugin/bin/lib/pr-bookkeeping/precondition.js` (#2472) is now a second consumer of the same three helpers via a phase-boundary CLI, `plugin/bin/check-pr-bookkeeping.js`. Review in wrap-up. | fixed | Added "Other consumers" note to docs/hooks.md's bookkeeping-stamps gate section |
| 3 | review | Step 3 Routing (Architecture, confirmed via direct reproduction): `check-pr-bookkeeping.js`'s new `/test` Step 0 gate is a silent no-op for multi-spec `/flow` runs — `hasMaterializeCommit`'s git pathspec assumes `runDir` is the parent run directory, but a multi-spec per-spec `/test` invocation receives `$PIPELINE_RUN_DIR` = `{parent}/spec-{N}/` (`flow/multi-spec.md`'s env-var table), so the pathspec never matches the real committed path and the check always resolves `not-materialized-yet` (fail-open, zero coverage). Deferred rather than same-pass patched — touches a shared, IL-131-relevant utility with another live caller (`checkBookkeepingStampsGate`) and needs its own fix+test cycle. | deferred | Filed as #2571 (defer-reason: genuinely-larger) |
| 4 | wrap-up | Reflect full mode, Near-misses lens: the Critical false-positive HARD-GATE bug (missing `worktree` field always treated as a violation, HARD-STOPping current-branch git-strategy runs) was an incomplete port of `checkBookkeepingStampsGate`'s linked-worktree scoping — caught and fixed by this run's own whole-branch review (commit 71d7bac05) with a regression test added. CAUSAL: terminal. | accepted | Already fixed and tested pre-merge; no further action |
| 5 | wrap-up | Reflect full mode, Friction lens: `friction-events.js` returned 10 `contract-violation` events from the SDD build phase, but each is the dispatching session's own inter-dispatch narration ("Waiting for Task N's implementer/reviewer to complete"), not a subagent reply lacking a status line — a false-positive class `subagent-stop.js`'s heuristic doesn't yet distinguish from a real violation, distinct from the existing #1596 third-party-agent exemption. | deferred | STAGED to decisions.md for Review Console; no backlog record filed this run (observation-only, no concrete fix proposed yet) |
