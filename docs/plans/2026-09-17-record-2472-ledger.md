# Open Items — pr-first run reached wrap-up with no PR/run-state and no logged degrade trace (#2472)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `pre-tool-use-gate-exemptions` skill's fail-closed-on-unprovable discipline is written for `pre-tool-use.js`'s own deny gates; `plugin/bin/lib/pr-bookkeeping/precondition.js` (#2472) reuses its `isLinkedWorktree`/`indeterminate` scoping pattern but deliberately inverts the posture to fail-open (a benefit-of-the-doubt precondition check, not a deny gate) — worth folding in as a documented sibling pattern next time that skill is touched. | observation | — |
| 2 | build/docs | docs/hooks.md may need a cross-reference note — it documents `checkBookkeepingStampsGate`'s internal helpers (`hasMaterializeCommit`, `hasLoggedPrDegrade`, `resolveRunPinnedIntegrationModel`) in depth but has no "other consumers" section; `plugin/bin/lib/pr-bookkeeping/precondition.js` (#2472) is now a second consumer of the same three helpers via a phase-boundary CLI, `plugin/bin/check-pr-bookkeeping.js`. Review in wrap-up. | open | — |
