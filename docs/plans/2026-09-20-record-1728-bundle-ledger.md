# Open Items — Record bundle 1728,1996,2268,2561,2562,2564,2636,2656,2658,2660

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Baseline `npm test` (unmodified base, pre-flight sweep) has 3 pre-existing failures in `tests/impeccable-plugin-contract.test.js` (#7329 "the installed plugin matches the pinned version", #7333 "gatherSignals() executes cleanly...", #7334 "the CLI entrypoint accepts no flags") — the installed Impeccable plugin versions in this environment (3.0.6, 4.2.0, 4.2.2, 4.3.1) do not include the pinned 4.0.2, an environmental/installation-state mismatch unrelated to any of this bundle's 10 records. | accepted | Pre-existing on unmodified base per pre-flight verify sweep (`multi-spec.md`'s Pre-flight Verify Sweep); not touched by any record in this bundle. Full log: `/private/tmp/claude-501/-Users-thomasholknielsen-Code-Workspaces-claude-tweaks/10bf52b8-aa50-41a0-94df-0f71d42bb945/scratchpad/preflight-test-baseline.log`. |
| 2 | test | Post-build full `npm test` surfaced 2 more failures in `tests/bin-lib/visual-decide/cli.test.js` ("status: never-started, running, stopped, crashed", "AC6 (CLI path): fractional --idle-minutes self-exits and status reflects stopped") — both expect the daemon's status to transition through crashed/stopped but observe `running` instead, on every re-run (isolated and full-suite). `visual-decide/`'s test server binds `127.0.0.1:4173`; `ps aux` at the time showed an unrelated sibling process, `next dev --turbopack -p 4173` from a different repo checkout (`Code Workspaces/dispatch/.claude/worktrees/mcp-server-registry-design`), already bound to that exact port — an external port collision, not a code defect. No file under `plugin/bin/lib/visual-decide/` or `tests/bin-lib/visual-decide/` appears in this bundle's diff (`git diff 3d735cc2d..HEAD --stat` for that path is empty), confirming none of the 10 records touched this module. | accepted | Environmental port collision with a concurrent sibling session's dev server, confirmed via `ps aux` and an empty diff for the module's own path; not caused by, or fixable within, this bundle. |
| 3 | test | Post-build full `npm test` surfaced 1 genuine regression from this bundle's own work: `judge-procedure.md body is self-contained` (`tests/bin-lib/harness-health/skill-md.test.js`) failed on the word "below" landing inside the inlinable body — introduced by #2656's dimension-10 addition (`a genuine guardrail the rule-expiry check below would independently confirm is still live`). | fixed | Removed the self-referencing "below" (`plugin/skills/harness-health/judge-procedure.md` line ~153); the guard doesn't need a positional pointer since the rule-expiry check is described elsewhere in the same inlined body. `tests/bin-lib/harness-health/skill-md.test.js` (40 tests) now green. |
| 4 | test | Post-build full `npm test` surfaced a second genuine regression: `source-registry.md dispatch uses Form B and the four-value status line` (`tests/research/skill-md.test.js`) failed — the status-line migration commit (`2b1d653f2`, #2268) had rewritten `source-registry.md`'s enumeration to `STATUS: DONE \| STATUS: DONE_WITH_CONCERNS \| STATUS: NEEDS_CONTEXT \| STATUS: BLOCKED`, breaking the test's bare `DONE \| DONE_WITH_CONCERNS \| NEEDS_CONTEXT \| BLOCKED` regex (each value now carried a repeated `STATUS: ` prefix). | fixed | Changed the line to `STATUS: DONE \| DONE_WITH_CONCERNS \| NEEDS_CONTEXT \| BLOCKED` — one leading label, bare pipe-separated values, matching the test's regex and the file's own "labeled `STATUS: {WORD}`" prose one line above. `tests/research/skill-md.test.js` (42 tests) now green. |
| 5 | review | #2561's new `fetchIssueTypeGraphQL()` (`plugin/bin/lib/compose-subject.js`) string-interpolated `repoSpec.owner`/`repoSpec.repo` directly into the GraphQL query text instead of binding them as `$owner`/`$repo` GraphQL variables via `-f owner=…`/`-f repo=…` — the established pattern this codebase's `gh-api-module-pattern` skill documents and `bin/lib/issues/native-dependencies.js`'s `fetchNativeDependencies` already follows (lens 3a Convention, medium). | fixed | Auto-applied per `review-auto-apply-ceiling: medium`: rewrote to bound `$owner:String!`/`$repo:String!` variables passed via `-f`, matching `native-dependencies.js`'s shape; added an argv-shape regression test. `tests/bin-lib/compose-subject.test.js` (33 tests) and `tests/merge-subject-composer-conformance.test.js` (7 tests) both green. Commit `50b6c57c8`. |
| 6 | review | Step 1 Spec Compliance (#2564): only Deliverable "the reusable fix mechanism" shipped (`plugin/bin/node-eval-file.js` + its test, both green, but unreferenced by any skill file). Deliverables 1-3 (restructuring `queue-pull-script.md`'s 27 inline `node -e` blocks and `next-ranking.md`'s equivalent blocks to actually use it) are NOT done — confirmed via `grep -rln node-eval-file plugin/skills/` returning only the tool file itself. All 5 Acceptance Criteria are consequently unmet (none are checkable without the migration). This is a significant/BLOCKED gap under Step 1's gate table, not a minor one — but it is the build phase's own disclosed, reasoned judgment call (extreme blast radius of a silent shell-escaping mistake in dispatch's core queue-pull path, with no real Windows Git Bash environment available to verify against), not a hidden shortfall. | open | Routed to wrap-up: #2564 stays OPEN (not closed via this run's merge/closing commit) with a comment documenting what shipped vs. what remains; the issue's own Deliverables/AC text already fully describes the remaining migration work, so no separate follow-up record is filed — `#2564` itself is the follow-up. |
