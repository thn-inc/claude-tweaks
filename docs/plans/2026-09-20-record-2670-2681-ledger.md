# Open Items — #2670, #2681 (agent-browser press + auth migration)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | test | Pre-existing `npm test` failures: 3 in `tests/impeccable-plugin-contract.test.js` (installed impeccable plugin version mismatch vs the 4.0.2 pin) — reproduced identically in isolation (`node --test tests/impeccable-plugin-contract.test.js`), unrelated to any file touched by this run's diff | accepted | Pre-existing and unrelated — tracked separately at #2531 ("impeccable-plugin-contract.test.js's version-pin test fails in a sandbox with a mismatched plugin cache"). Verified independently before and after spec #2670's commit; same 3 failures both times. |
| 2 | build | #2681 shipped the QA-story-authoring path off `agent-browser` auth (`qa-agent.md`, `stories/auth-resolution.md`/`SKILL.md`/`refine.md`/`story-examples.md`) but **not** the `/test qa` orchestrator's own live invocations: `plugin/skills/test/qa-prompts.md` (Phase 3 prompt template, lines ~96-97, ~152) and `plugin/skills/test/qa-procedures.md` (Phase 2.5 pre-flight, lines ~99-112) still instruct `agent-browser auth login`/`auth list`/`auth save`; `plugin/skills/_shared/dev-url-detection.md` line 28 also still references `agent-browser auth save`. Independently confirmed via `grep -rn agent-browser plugin/skills/test/qa-prompts.md plugin/skills/test/qa-procedures.md plugin/skills/_shared/dev-url-detection.md`. These files were outside #2681's declared Current State/Deliverables/Key Files — out of surgical scope for that record — but this means #2682's premise ("once #2670 and #2681 ship, nothing in the plugin calls agent-browser anymore") is not actually true yet: the `/test qa` production pipeline still depends on agent-browser's auth commands. | deferred | Staged as a new backlog record proposal for Review Console approval — see `staged/backlog-1.md`. Genuinely separate file scope from #2681's declared Key Files, not introduced by this build — defer, not fix-in-place, per leftover-default policy. |
