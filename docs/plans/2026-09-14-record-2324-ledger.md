# Open Items — Release bootstrap: explicit --release-type / --extra-file override

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `plugin/bin/release-bootstrap.js`'s CLI wrapper is a plain `main(argv)` with `bootstrapRelease`/`fs.statSync` called directly, not the `.claude/skills/gh-api-module-pattern` skill's documented `run(argv, deps)` shape (every side effect through `deps` so tests inject fakes) — pre-existing from #2253, unchanged by this build, which only added two flags to it | observation | Pre-existing convention gap, not introduced here; the skill may be stale for this file's era, or the file is a migration candidate — noted for `/wrap-up`'s skill curation, no action taken in this build |
| 2 | review/3b | `--extra-file` was joined onto `root` with no path-containment check, letting a `../`-laden value resolve outside `root` and persist verbatim into the rendered config | fixed | Added an explicit resolved-path containment check in `resolveReleaseType`; commit `97f110deb` |
| 3 | review/3c | `resolveReleaseType`'s override validation collapsed 6 distinct `--extra-file` failure causes into one identical error message, discarding `readJson`'s own error/parsed distinction | fixed | Added `extraFileProblem()` to report each cause distinctly; commit `97f110deb` |
| 4 | review/hindsight | Missing consolidation: the new `--extra-file` containment check (item 2) was a 6th independent copy of the "is candidate inside root" idiom, already reimplemented separately in `reap-merged.js`, `worktree-reap.js`, `transcript.js`, and `pre-tool-use.js` (2 call sites) | fixed | Extracted `isPathContained(candidate, root, {orEqual})` into `shared-primitives.js` (following #977's consolidation convention) and migrated all 6 call sites, preserving each site's exact orEqual/strict semantics; commit `db1f5fa66` |
