# Open Items — wrap-up-pack.js: ledger probe reports 0 items for a slug-named ledger file (#2563)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | review | `ledgerProbe`'s new multi-candidate resolution in `plugin/bin/lib/wrap-up/pack.js:394` does not implement Deliverable 1's stated topic/spec-slug matching — it only adds `candidates.length <= 1 ? candidates : candidates.filter(namesRecord-only)`. Two confirmed effects, reproduced independently across lens 3b (Security, both agents), lens 3c (Error Handling, both agents), and corroborated by lens 3f (Test Quality, both agents) against `plugin/bin/lib/wrap-up/pack.js`: (a) **regression** — the `candidates.length <= 1` branch removes the pre-existing `namesRecord` ownership check entirely for the single-candidate case (the pre-diff code always required a `namesRecord` match, even at n=1), so a lone leftover/orphaned ledger from an unrelated, uncleaned prior run (a real, documented occurrence per `plugin/skills/tidy/scan-procedures.md`/`step-6-auto.md`) is now silently adopted as this run's own and its counts folded into Phase 3's wrap-up gate with zero verification; (b) **incomplete fix** — the `candidates.length > 1` branch is byte-identical to the pre-diff code (still `namesRecord`-only), so when 2+ ledger files coexist in `docs/plans/` and none embeds a record number (the realistic case docs/plans/ accumulates over time — see sibling ledgers already present in this same worktree), the original #2563 defect (0-item report, Phase 3 gate silently passing on genuinely-open items) reproduces exactly as before, just narrowed to the multi-candidate case. Neither AC2 nor AC3 as literally written exercises this combination (both cover already-working record-number-named scenarios), but Deliverable 1's stated general solution is not met, and the spec's own Gotchas section explicitly anticipated this: "If the run's topic/spec-slug association isn't reliably available to `ledgerProbe` at the point it runs (e.g. no run-state or plan metadata carries it), the fallback behavior needs to be decided explicitly rather than silently reverting to the broken record-number match — flag this as an open design question if the association turns out not to be readily available in `pack.js`'s existing `inputs`." No such flag was raised; `inputs` (`resolveInputs`, pack.js:266-301) carries `runDir`, `records` (issue numbers), and `worktree`, but no plan/spec **topic-title** field a candidate filename's `{feature}` component could be matched against — the run directory's own slug (dispatch-minted `record-{n}`/`spec-{n}`) is not reliably the same string as the ledger's topic-derived `{feature}` slug, so a substring/slug match against `runDir`'s basename is not a safe drop-in without a design call on what "reliably available" means here. Test-quality lenses (3f-a, 3f-b) independently confirm no test exercises either gap: the new AC1/AC4 tests only cover a single genuine candidate, and no test places 2+ topic-slug-named candidates or a lone unrelated candidate. This finding could not be resolved with a mechanical staged patch — both the safe-looking fixes (require `namesRecord` unconditionally, or trust any lone candidate) each re-break one of the two properties (the original bug vs. the misattribution regression) the other is meant to guard — it needs the design call the spec itself flagged as open. `Defer-reason: needs-human-decision`. | open | — |
| 2 | wrap-up | Reflect Friction lens: 5 `contract-violation` events this run, all this dispatched agent's own interim progress-update replies between async nested review-lens dispatches (not final replies) — the hook's contract-violation detector has no way to recognize a legitimate mid-dispatch interim turn distinct from a real final-reply contract breach. `[route: D5]`. Staged at `staged/reflect-1.md`. `Defer-reason: tangential` (about claude-tweaks' own hook machinery, not this record's own change). | deferred | Routed D5 (upstream) — awaits Phase 2's Upstream feedback row / Review Console approval. |
