# Open Items — #1936, #1996, #2063, #2265 (file-overlap bundle)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | #1996 build could not proceed — its own Deliverable 1 requires a posted before/after subagent-read measurement on #1995 that does not exist (5th consecutive identical finding, retry ceiling of 3 exceeded) | accepted | Settle removed `auto:build`, added `bot:blocked` on #1996 (decisions.md 08:23:25). Needs human re-triage: confirm the #1995 measurement exists somewhere unchecked, or close #1996 per its own not-needed branch. Not part of this run's review/polish/wrap-up scope. |
| 2 | review | #2265's STAGED build decision (decisions.md 08:28:11) cites commit `dc189e90c` as one of two commits that shipped the spec on main via v6.122.0. Re-verified: `dc189e90c` exists only on a sibling worktree branch (`worktree-record-1936-1996-2008-2063-2080-2282-2332-2344-2345`), not merged to main; only `546d45182` is actually an ancestor of this branch's HEAD. The underlying substantive claim is still correct — 5/5 STATUS: mentions present in subagent-output-contract.md, two-tier `detectStatus()` present in subagent-stop.js, content verified directly — so no code fix is needed, only the commit citation was stale/wrong. | observation | No action required — informational only, does not change the no-op verdict for #2265. |
| 3 | review | #1936 (contract-retries.js + subagent-stop.js forced-retry) and #2063 (step3-lens-dispatch.md trim) both re-reviewed directly against their diffs: forced-retry counter degrades safely on any read/write failure (never causes a block-loop), targeted suite `tests/bin-lib/hooks/contract-retries.test.js` + `tests/hooks-subagent-stop.test.js` re-run clean (22/22); #2063's trim verified against a live grep — no test pinned the removed phrasing. No findings. | observation | No action required. |
| 4 | test | Full `npm test` re-executed in the foreground as a raw artifact (no stamp matched HEAD `9069c9e01`): 9226 pass, 3 fail — all three in `tests/impeccable-plugin-contract.test.js` (installed Impeccable plugin version drift: 3.0.6/4.2.0/4.2.2/4.3.1 installed vs pinned 4.0.2), unrelated to this run's changes. | accepted | Pre-existing environment drift, unrelated to this bundle's diffs (subagent-stop.js, contract-retries.js, step3-lens-dispatch.md touch none of the Impeccable-pin machinery). |
