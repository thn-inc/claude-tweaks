# Open Items — Multi-spec run #2697, #2757, #2758, #2759

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Pre-flight branch-divergence-check: origin/main was 698 commits ahead of local main (6.121.0 → 6.129.0) at run start; auto mode continued (reason-not-auto: n/a — resolved mechanically) | observation | Post-creation catch-up merged origin/main into the shared worktree (merge commit `6d0f768a4`); the run builds on 6.129.0 |
| 2 | test | Batch pre-flight verify sweep on the unmodified base `6d0f768a4`: `node tools/run-tests.js` → 9353 tests, 9006 pass, 342 fail, 5 skipped, across 103 test files (full per-file list: `{run-dir}/sweep-baseline.txt`). Windows-environment baseline (CRLF/path-shaped failures — hooks-git-command, cloud-setup-drift-check, verify-scope-starter, archive-merged, …), consistent with the machine's moving baseline (292 fail @ 7948 tests on 2026-09-07); not caused by any spec in this run. Per-spec `/test` steps diff their failing-file set against this list and cite `Pre-existing — see ledger #2, batch pre-flight sweep` rather than re-diagnosing | open | — |
| 3 | build | Follow-up (#2697's whole-branch review recommendation, out of scope): the prose-conformance suites read skill files raw, so any test written on a Windows box is one `git checkout` (CRLF smudge under `core.autocrlf=true`) away from joining ledger #2's baseline — `tests/skill-mode-split-conformance.test.js` was red at HEAD for exactly this until its `read()` normalized `\r\n`; `tests/feedback-watermark-prose.test.js:190` fails the same way today. Proposal: one shared EOL-normalizing read helper (or a `.gitattributes` `* text eol=lf` policy) for `tests/*-conformance*.test.js` and siblings | deferred | → backlog — auto-routed by the resolve gate's narrowing (`autonomy: unattended`, Defer-reason: genuinely-larger); record proposal staged at `spec-2697/staged/ledger-record-eol-normalizing-read-helper.md` for the consolidated console's Queue writes (born-ready) |
| 4 | review/hindsight | [skill: build/plan-authoring-checks] Plan test-discovery for a prose move must grep `tests/` for the moved sentences, not the file path — #2697's plan missed two content pins (feedback dedup-basis, Gather 2 Skip-check) that went red at Task 5; add a "Moved-prose pin check" to `plugin/skills/build/plan-authoring-checks.md` (D2, staged as `spec-2697/staged/reflect-1.md` for the Review Console → wrap-up Skills curation) | observation | Staged for the Wrap-Up Review Console; not applied in-run (out of #2697's scope) |
| 5 | ops | `plan-audit.js` Check A flagged `plugin/skills/activity/SKILL.md` (nearest existing ancestor `plugin/skills`): a brand-new single-file skill directory has no sibling `Create:` to "bring the parent into existence", so the rule in `build/plan-audit.md` line 64 reads a correct plan as a typo. Ruling (unattended, `[IL-71]`-style premise check): the path is intentional — #2757 creates the `activity` skill — and no revision of the plan can satisfy the check short of a fake sibling file or a pre-made empty directory, both worse than the finding; Check B/C/D and headroom passed. Proceeded | observation | Hindsight for `/reflect` → `build/plan-audit.md` + `bin/lib/plan-audit/checks.js`: accept a `Create:` whose missing parent is itself named as a directory the plan creates (e.g. a `Create: plugin/skills/{name}/` bullet), or special-case `plugin/skills/{new}/SKILL.md` |
| 6 | build/skill | [skill: gh-api-module-pattern] Enrichment: GitHub's search API (`pr list --search`, `issue list --search`, `search prs`) does not follow a repository rename/transfer redirect while REST (`gh api repos/...`) does — a stale `origin` slug silently returns zero rows or "Invalid search query". `plugin/bin/activity-gather.js` canonicalizes every slug via `gh repo view {slug} --json nameWithOwner -q .nameWithOwner` before querying; the skill's repo-slug paragraph should name this as the rule for any search-backed call | observation | Staged for the Review Console's Skills curation row (build/skill); not applied in-run |
| 7 | review | [lens 3c] `plugin/bin/activity-gather.js:150` — a `--repo` value whose entries all filter to blank (`--repo ","`) leaves `repos = []`, so `attempted = 0 === failures.length` and `facts.failures[0].error` throws an uncaught TypeError, escaping the 0/1/2/3 exit vocabulary (both reproduction agents reproduced it live; confirmed by direct read) | fixed | Fixed at f7f646756 alongside #8 — rejecting blank entries makes the zero-entry path unreachable; `--repo ","` → exit 1 pinned in `gather-cli.test.js` |
| 8 | review | [lens 3c] `plugin/bin/activity-gather.js:89` — blank `--repo` segments from stray commas are silently dropped instead of failing the invocation (low) | fixed | Fixed at f7f646756 — a blank `--repo` segment is a usage error (exit 1) |
| 9 | review | [lens 3b] `plugin/bin/activity-gather.js:39` — `REPO_RE` accepts a literal `.`/`..` repo segment that would be interpolated into the REST path `repos/{slug}/commits` (mitigated today by the mandatory `gh repo view` canonicalization, which fails first; defense-in-depth per `gh-api-module-pattern`) (low) | fixed | Fixed at f7f646756 — a `.`/`..` repo name is a usage error (exit 1) |
| 10 | review | [lens 3b] `plugin/bin/activity-gather.js:107` — the origin-derived slug never passes `REPO_RE` before its positional use in `gh repo view` at :125; `parseRepo`'s `[^/]+` capture accepts an owner segment starting with `-`, the flag-injection class `gh-api-module-pattern` pins (high) | open | Staged as staged/review-1.patch (Review Console) — REPO_RE applied to the origin-derived slug, exit 2 with a pointer to --repo when it fails |
| 11 | review/hindsight | [skill: build/plan-authoring-checks] Task 0 live probe used the hand-picked canonical slug, not the origin-derived value the CLI resolves — the search API rename-redirect gap survived to Task 7 (staged `spec-2757/staged/reflect-1.md`) | observation | Staged for the Wrap-Up Review Console; not applied in-run |
| 12 | review/hindsight | [skill: build/plan-authoring-checks] New-skill plan task named four suites but not the skill-audit pins (`context-cost` description ceiling/corpus budget, `csc-registry` standalone exemption) — both bit this build (staged `spec-2757/staged/reflect-2.md`) | observation | Staged for the Wrap-Up Review Console; not applied in-run |
| 13 | review/hindsight | [skill: NEW - fact-validated-narration] gather → narrate → render with citation validation, distinguishable warning forms, free-text neutralization, and truncation/partial signals is a reusable boundary no project skill documents (staged `spec-2757/staged/reflect-3.md`) | observation | Staged for the Wrap-Up Review Console; not applied in-run |
| 14 | ops | `plan-audit.js` Check A flagged `plugin/skills/walkthrough/SKILL.md` (nearest existing ancestor `plugin/skills`) — same false positive #2757 ruled on (ledger #5): a brand-new single-file skill directory has no sibling `Create:` to bring the parent into existence. Ruling (unattended): the path is intentional — #2758 creates the `walkthrough` skill — Check B/C/D and headroom passed. Proceeded | observation | Same hindsight as ledger #5 — accept a `Create:` whose missing parent is itself named as a directory the plan creates |
| 15 | build | #2758's SDD whole-branch review (opus, range d509698be..0e4380346) found "Ready to merge? No": Critical — `plugin/bin/lib/gif/lzw.js`'s mid-stream table reset wrote its clear code at the post-reset (narrow) width instead of the pre-reset (wide) width, permanently desyncing the bitstream; verified by decoding the build's own live-run GIF and finding both real 1280x720 frames undecodable partway through. Critical — the test claiming to cover the reset path never actually forced one (max table index 491 of the 4096 threshold). Plus Important: `palette.js` at 224s/900MB RSS for one real walkthrough; unvalidated numeric CLI flags silently disabled the budget guard via NaN; `captionList` had no production caller. Plus Minors (locator=? caption fallback gap, PNG truncation, frame-length validation, etc.) | fixed | Fixed at 41dc8424f — `lzwEncode` now emits the reset clear code at the pre-reset width; `palette.js` rewritten for distinct-color-dedup + cached nearest-index performance; `captionList` given a real `--steps-json`/`--captions` CLI caller; numeric flags validated. Independently re-reviewed (opus, own from-scratch GIF89a decoder, real `frame-01/02.png` screenshots): Critical #1/#2 confirmed genuinely closed — real screenshots exercise 4 mid-stream resets across both frames, decode bit-exact (921,600/921,600 px, 0 mismatches); the pre-fix `out.gif` fails the same decoder with a bitstream underrun. Combined 7-suite run: 56/56 pass |
| 16 | review | Re-review found two residual defects the fix wave introduced/left open: `--steps-json` with a syntactically-valid but non-array JSON payload (object/null/string/number) reached `captionList` unguarded and threw an uncaught TypeError, escaping the CLI's 0/1/2/3 exit-code vocabulary; `--width` validation checked `Number.isFinite && > 0` but not integer-ness, so `--width 640.5` passed validation and produced a structurally corrupt GIF (decoded pixel count overran the declared dimensions) | fixed | Follow-up fix dispatched (sonnet): `--steps-json` now requires the parsed JSON to be an `Array` before calling `captionList`, exiting 1 otherwise; `--width` validation now requires `Number.isInteger` in addition to finite/positive. Also renamed a misleadingly-titled LZW regression test that claimed to re-derive the old buggy encoder's behavior but only ran a second round-trip check |
| 17 | review | Re-review measured `palette.js`'s rewrite is distinct-color-count-bound, not pixel-bound: fast on the real screenshot fixtures (58ms at 1280x720), but an adversarial 8-frame 1280x720 gradient+noise walkthrough (259,895 distinct colors) took quantize=482,135ms / rss=1,336MB — worse than the original bug. UI screenshots sit in the fast band (low thousands-tens of thousands of distinct colors); a walkthrough over a photo/map/video-heavy page would hit the cliff. The shipped perf regression test (400x300, <5000ms budget, measures 730ms) does not bound this case | deferred | Not fixed in-run — genuinely-larger scope than a follow-up patch (would need a color-count-based sampling/pre-bucketing strategy). Staged as a backlog candidate for the Wrap-Up Review Console's Queue writes (defer-reason: genuinely-larger) |
| 18 | test | #2758's Common Step 5 full-suite run (`node tools/run-tests.js`, HEAD `7d8af53a8`): 9499 tests, 9144 pass, 350 fail, 5 skipped — up from ledger #2's baseline (9353/9006/342/5) by +146 tests (new suites from this run's four specs) and +8 fail. Root-caused all 8: 5 are `tests/bin-lib/skill-audit/context-cost.test.js`'s Windows-vs-POSIX path-separator assertions (same class as #2/#3's baseline — `\\` observed where fixtures expect `/`); 1 is that same file's composed-bytes ceiling check (`worktree-setup @ build\SKILL.md:170: 45.5 KB > 45.0 KB`), traced to commit `522f8b6d1` (record #2348, already on the branch ahead of the run's own `6d0f768a4` base — not caused by #2758's only edit to that file, an unrelated description-char-ceiling bump); 2 are `tests/demo-visual-decision-adoption.test.js` and `tests/research/skill-md.test.js`'s two assertions, none touching walkthrough/gif code — the former traced to commit `b8fd015d9` (**#2697's** own demo-mode-split extraction, already wrapped), the latter unrelated to any spec in this run. None of #2758's own suites (`tests/bin-lib/gif/*`, `tests/bin-lib/walkthrough/*`, `tests/walkthrough-conformance.test.js`, `tests/stories-caption-expand-only.test.js`) or AC10's named suites appear in the failing set | open | Common Step 5 recorded as passed for #2758 (baseline-class and sibling-spec-caused failures aside, per ledger #2's own precedent). The `522f8b6d1`/record-#2348 byte-ceiling breach and the `b8fd015d9`/#2697 demo-visual-decision regression are real, un-fixed defects on the shared branch that predate or fall outside #2758's scope — flagging here for the run's final consolidated gate or a follow-up backlog record; not fixed in-run |
| 19 | ops | #2758's wrap-up (Phase 4, Multi-spec defer protocol step 4, `wrap-up/review-console.md` lines 41-53) instructs writing `spec-2758/verify-expectations.json` (`{"version":1,"memory":[],"upstream":[],"deferred":[...]}`) into the pipeline run dir. No sanctioned writer CLI exists for this file — `gh-api-module-pattern/cli-wrapper-contract.md` names exactly three run-dir writers (`log-decision.js` → `decisions.md`, `stage-item.js` → `staged/`, `set-config.js` → `config.yml`); a direct `Write` to the main-checkout run-dir path was attempted and refused by this session's worktree-isolation gate ("Edit the worktree copy of this file instead of the shared-checkout path"). The file was therefore NOT written for spec-2758. Documented, bounded consequence per `review-console.md` itself: a per-spec `verify` run against this spec renders `memory-updates`/`upstream-feedback` as `unknown (expectations file missing)` instead of the intended `skip (nothing recorded)` — a "known, accepted limitation... not a full resolution" in the skill's own words. Does not block the run's final consolidated Review Console (which reads each spec's `decisions.md`/`staged/` directly, not `verify-expectations.json`). Flagging here for the run's final wrap-up (which can write it once outside worktree isolation, or for a backlog record proposing a fourth sanctioned writer CLI for this file class) | deferred → backlog | #2759's wrap-up (the run's actual final spec) re-confirmed: still worktree-isolated, still cannot write the file — the first option in this row's own Resolution never became available. Phase 1 fix-exhaust: not fixable now (same blocker). Blocker reclassified `Defer-reason: blocked-dependency` (the fix waits on functionality not yet built — a fourth sanctioned writer CLI) — clears the `ledgerNarrowing` floor at this project's `autonomy: unattended` ceiling; auto-routed to `Route to a record → Keep (backlog)` with zero per-item drill, per `_shared/autonomy-ceiling.md`'s Bookkeeping capabilities table. Staged: `spec-2759/staged/ledger-record-19.md` |
| 20 | review/hindsight | [skill: NEW - mirror-surface enumeration] #2759's whole-branch review found a THIRD `--upstream` mirror surface (`help/reference-card.md`'s Takes column, pinned by `tests/reference-card-argument-hint.test.js` under #564) beyond the two #679 named — caught only because the full-suite gate ran per-file, not on totals (staged `spec-2759/staged/reflect-1.md`) | deferred | Staged for the run's final consolidated Review Console; not applied in-run |
| 21 | review | `tests/intake-conformance.test.js:28`'s verdict-table boundary scan uses a bare-LF `SKILL.indexOf('\n\n', ...)` against a non-normalized read, so it fails on any CRLF checkout regardless of content — proven environmental across two independent checks this run (Task 5's review, and this fix wave's own combined-suite run), at baseline both times (staged `spec-2759/staged/reflect-2.md`) | deferred | Staged for the run's final consolidated Review Console; not applied in-run |
| 22 | build | #2759's whole-branch review found Critical C1 (`feedback/SKILL.md` Step 2's unconditional "if not D5, stop" clause sat before the `--upstream` routing paragraph, making the entire `--upstream` dispatch unreachable by the skill's own linear prose — confirmed live via a probe agent), plus Important I1 (a new mid-flow `AskUserQuestion` in `auto` mode, no covering exemption) and I2 (contradictory `--upstream`/`--dry-run` Step-skip claims), plus 10 Minors | fixed | Fixed at `af698b213`: `--upstream` routing now checked before the D5 stop clause; `AskUserQuestion` gated to interactive mode only; dry-run/upstream interaction clarified; 4 cheap minors fixed. Independently re-reviewed (opus): C1 confirmed genuinely closed (dispatch reachable, no regression on the plain-stop case), but found 3 residuals — `kind` undefined on the new path, 3 sub-issues in the I1 fix, no regression test pinning C1's ordering. Second fix round (`d67caf6b5`) closed all 3: `learning-routing.md` gained a dedicated third-party classification rule; `upstream-draft.md`'s auto-mode prose, false-MISMATCH risk, and writer citation all fixed; a discrimination-proven regression test now pins Step 2's paragraph ordering. Combined suite: 95/99 pass, 4 pre-existing baseline failures only |
| 23 | build | `feedback/SKILL.md` is now at 30,718/30,720 CRLF-normalized bytes — 2 bytes of headroom — after #2759's required Item-1 fix (a new "Third-party `--upstream`" bullet in Step 2's Otherwise list). Item 4 (a minor I2 wording tightening) was deliberately skipped in-run because there was no room left for it | deferred → backlog | #2759's own wrap-up (the run's final spec): Phase 1 fix-exhaust — not fix-now eligible (a split or ceiling raise is out of this spec's own diff, spans unrelated systems). Blocker classified `Defer-reason: genuinely-larger` — clears the `ledgerNarrowing` floor at this project's `autonomy: unattended` ceiling; auto-routed to `Route to a record → Keep (backlog)` with zero per-item drill. Staged: `spec-2759/staged/ledger-record-23.md` |
| 24 | ops | #2759's wrap-up (final spec) residue sweep (`bin/residue.js --scope blast-radius --own-pr 2760`, base `c3a0057f024a9780e736faf3f4e1b195807835d6`): worktrees/branches/forge/pipeline-runs/artifacts probes all clean (zero findings). Forge probe reports this run's own pr-first PR | observation | PR #2760 (`worktree-flow+spec-2697-2757-2758-2759` → `main`), draft, mergeStateStatus CLEAN; disposition is Phase 4's merge decision (a separate, later step — not this wrap-up's) |
| 25 | ops | #2759's wrap-up residue sweep: suite probe reported `ran: false, reason: "skipped via --no-suite"` (pack-fed, per `residue-sweep.md` — a fact pack never re-runs the suite). No `claude-tweaks-verify-pass.json` stamp exists for this worktree to read the real state from instead; the only `report.json` found (`{git-dir}/claude-tweaks-verify/report.json`) is stale — `sha: 7d8af53a8`, `dirty: true`, `pass: false`, timestamped mid-#2758's own Common Step 5, not #2759's | observation | Actual suite state for #2759 taken from its own `decisions.md` "Common Step 5" entry instead: 387 tests, 374 pass, 13 fail, every failing file at or below the ledger #2 baseline — zero regressions |
| 26 | ops | #2759's wrap-up residue sweep: release-triple probe reported `ran: false, reason: "could not read CHANGELOG.md or docs/shipped-versions.tsv at HEAD"`. Root-caused: `CHANGELOG.md` reads fine at HEAD; `docs/shipped-versions.tsv` does not exist anywhere in this worktree (`find . -iname "shipped-versions*"` — zero matches) — the release-triple convention's third leg is entirely absent from the repo, not just missing an entry for this version | observation | Out of #2759's scope to diagnose or fix (surgical-changes rule). Flagging for the run's final consolidated gate / a follow-up backlog record: either the convention was retired without updating `bin/lib/residue/probes/release.js`, or the file was lost |
| 27 | wrap-up | [skill: build/plan-authoring-checks] Batch reflect, Near-misses lens (`CAUSAL: systemic`): a test that pins the **existence** of a threshold-gated or order-gated branch, without forcing the condition that activates it, passes while that branch is broken. Two independent instances in one run — #2758's `lzw.js` mid-stream reset (the covering test peaked at table index 491 of the 4096 threshold; ledger #15) and #2759's `feedback/SKILL.md` Step 2 clause ordering (every conformance pin asserts presence, none ordering; ledger #22). Proposal: a "Reachability check" in `plugin/skills/build/plan-authoring-checks.md` requiring a gated branch's test to reach the gate and assert on post-gate output | observation | Staged for the run's consolidated Review Console at `{parent-run-dir}/staged/reflect-1.md`; not applied in-run |
| 28 | wrap-up | Batch reflect, Near-misses lens: a **fourth** CRLF-class conformance failure this run — `tests/demo-visual-decision-adoption.test.js:208` looks up a bare-LF `'## demo\n'` against a non-normalized `readFileSync`, so on this CRLF checkout the section slice is empty and the assertion fails regardless of content (verified live: the literal resolves at offset 42021 only in its CRLF form; the row it looks for is present at `docs/skill-graph.md:187`; 10/11 in-file pass). This **corrects ledger #18's attribution** of that failure to #2697's `b8fd015d9` — it is ledger #3/#21's environmental class, not a regression from this run. Ledger #18's *other* claim is confirmed: `522f8b6d1` is an ancestor of this run's base `c3a0057f0`, so the `context-cost` composed-bytes breach genuinely predates the run | observation | Staged at `{parent-run-dir}/staged/reflect-2.md`; strengthens ledger #3's staged EOL-normalizing-read-helper backlog proposal (four call sites, not two). Recorded as a new row deliberately — ledger #18 itself is left untouched for the run's own per-item gate |
| 29 | wrap-up | [skill: test] Batch reflect, Approach lens: a newly-red test file was attributed by **commit proximity** (`b8fd015d9` was the one commit touching `demo/`) rather than by reading its failure mechanism — the assertion's own message (`actual: ''`) already named a broken *lookup*, not missing content. The run's working counter-half is ledger #20 (compare failing-file sets per file, never totals). Proposal: one clause in `plugin/skills/test/SKILL.md`'s baseline-adjudication step — attribute a newly-failing file by reproducing its failure mechanism first; a commit touching the same directory is a hypothesis, not evidence | observation | Staged at `{parent-run-dir}/staged/reflect-3.md`; not applied in-run |
| 30 | wrap-up | Batch reflect, Surprises lens + tradeoff review (environment fact, D4 memory-bound): the full `node tools/run-tests.js` sweep crossed the Bash tool's 600s foreground ceiling **during this run** — 196.6s at #2697 (`d5343fdf7`), completed at #2757, over the ceiling by #2758 (confirmed independently three times), skipped outright at #2759 in favour of a 39-file scoped-combined suite. The run's own +146 tests are what moved it across; the existing `windows-npm-test-invocation` memory note no longer covers it. Consequence: the run ends with **no full-suite datapoint at its final HEAD** — the last is #2758's at `7d8af53a8` | deferred | Staged at `{parent-run-dir}/staged/reflect-4.md` as a proposed amendment to the existing memory entry (not a new file). The batch registry pass's **Memory row** then consumed this insight (no earlier row claimed it) and staged the concrete proposal at `{parent-run-dir}/staged/wrap-up-memory-1.md` — the full appended `**Update 2026-09-22:**` section plus the replacement `MEMORY.md` index line, since a memory file with no index entry is never loaded. Deduped: `windows-npm-test-invocation.md` already exists, so this extends it rather than creating a new file, and the existing index line's fixed "242-failure baseline" is itself stale (the file's own 2026-09-07 update already re-measured it at 292). The write is gated separately at the Review Console's Memory updates section — staging is not approval |
| 31 | wrap-up | Batch curation pass, post-fan-out shadow sweep (`hooks.js sweep-shadow`, run once against the parent run dir and once per `spec-{N}/`): 7 collisions, and **two of them were silent data loss, not duplicates**. `spec-2757`'s five (`build-deviation-1/2`, `reflect-1/2/3`) are byte-identical to their anchored copies — harmless. `spec-2758`'s two are **different findings under the same filenames**: the anchored `build-deviation-1.md`/`build-deviation-2.md` (written 11:13 — the skill-graph reverse-edge and AC7 no-running-app deviations) had silently overwritten the earlier premise corrections that `spec-2758/decisions.md` 07:59:24 records under those exact names (agent-browser → playwright-cli tooling correction; the inline Interaction-style directive that `skill-conventions.test.js` has forbidden since #1909). Both survived only in the worktree shadow and were recovered non-destructively as `*.shadow-dup`. #2758 therefore has **5** distinct staged proposals, not the 3 the anchored directory showed — the consolidated Review Console would have rendered 3 | observation | Recovered, nothing lost. All 7 logged individually to the parent `decisions.md` as `STAGED … shadow sweep` lines, each stating whether it is a duplicate or a divergence; the two recovered `spec-2758` files are real Review Console items. Apparent root cause: the staged-file counter restarts per writer rather than per run dir, so a later writer reuses `build-deviation-{n}` — worth a follow-up record against `stage-item.js`'s id allocation, out of this pass's scope |
| 32 | wrap-up | Batch registry pass, Decision records row: the judge's returned payload named an absolute, correctly-anchored `stagePath` (`{parent-run-dir}/staged/wrap-up-adr-1.md`) and reported verifying it, but **the file did not exist** — not in the anchored `staged/`, not in the worktree shadow, and not in the shared scratch directory (`find` over the whole checkout: zero matches). The parent shadow sweep had already run clean, so it was never written to either location. Re-prompted once per `curation-engine.md` §4's controller-side rule, with the absolute path and the sanctioned-writer command spelled out and raw command output required | fixed | The single permitted re-prompt succeeded: `{parent-run-dir}/staged/wrap-up-adr-1.md` now exists, 9,367 bytes, and its content was read end-to-end and confirmed genuine — the full `Target:`/`Invariant:`/`Finding:`/`Staged-at:` preamble, then a complete record with five alternatives each carrying a stated rejection reason (npm/`gifenc`, Playwright video recording, `ffmpeg`/ImageMagick, APNG, burned-in captions), a Consequences section quoting this run's own measured costs (the LZW clear-code width bug; the quantizer's 259,895-distinct-color cliff at 482,135 ms / 1,336 MB), an explicit reversibility paragraph, and a Revisit clause. Not a stub. The Decision records row recorded `1 staged` at 13:11:03Z; `render --section trace --strict` then exited 0, so no worklist row has a hole. Standing lesson for `curation-engine.md` §4: a judge's self-reported `test -f` is not evidence — the controller's own listing is |
