# Open Items — record #2436 (npm test: 3 sandbox-only failures split out of #1853)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build | `tests/impeccable-plugin-contract.test.js`'s "the installed plugin matches the pinned version" test fails (not skips) in any sandbox whose plugin cache carries a *different, present* version of `impeccable-plugin` than `tools/upstream-drift/manifest.yml`'s pin — this is the drift-detector working as designed (absent → skip; present-but-wrong-version → breach/fail), not a code bug. Could not be reproduced from this checkout without mutating the real `~/.claude/plugins/cache`, which the test file's own comments explicitly forbid. | deferred | Filed as #2531 (Review Console Queue writes, Apply default @ unattended). |
| 2 | wrap-up | Reflect (Fresh start, light mode): the ~10-line try/catch+skip guard for reading a historical git-show fixture is now duplicated across `tests/shaping-mode-needs-removal.test.js` and `tests/tidy-needs-worklist-rule.test.js`; extraction to a shared helper declined as premature for 2 call sites. | accepted | Left keep-staged (observation, no Title:/Type:/Labels: header — not a record-creation candidate). Superseded in substance by the applied Skills-row fix (commit 31bc4a2e6), which documents the underlying convention. Staged: staged/reflect-1.md. |
| 3 | wrap-up | Reflect (Friction, light mode): `/claude-tweaks:dispatch`'s task-prompt foreground-execution wording doesn't distinguish shell `run_in_background` from the `Agent` tool's own async notification mechanism, causing this run's reviewing session to redundantly duplicate Step 3's lens-dispatch analysis synchronously as a hedge. | deferred | Filed as #2533 (Review Console Queue writes, Apply default @ unattended). |
| 4 | wrap-up | Docs row (verified independently, corrected from the judge agent's own overstated framing): 5 other test files (`tests/session-limit-degrade-conformance.test.js`, `tests/needs-worklist-rule-cross-consumer.test.js`, `tests/untrusted-record-content-conformance.test.js`, `tests/demo-visual-decision-adoption.test.js`, `tests/demo-full-verification-pointer.test.js`) call `execFileSync('git', ['show', {historical-sha}:{path}])` unguarded — but, unlike #2436's original bug, each call sits *inside* a `test()` callback, not at module top-level, so a shallow clone makes only that one test FAIL outright (not a whole-file crash). Pre-existing, outside this diff's own two files. | deferred | Filed as #2532 (Review Console Queue writes, Apply default @ unattended). |
