# Open Items — This repo's migration to the new release process (first consumer)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | ops | Create a fine-grained GitHub PAT (`contents: write` on `thomasholknielsen/claude-tweaks-marketplace`) and store it as the `MARKETPLACE_TOKEN` secret on this repo, before `mirror-marketplace.yml` can succeed (reason-not-auto: requires-signoff — a human must create and scope a credential with write access to a separate repository; no CLI available to this build can provision that token on the user's behalf) | open | — |
| 2 | ops | Create a PAT (`contents: write` + `pull requests: write` on `thomasholknielsen/claude-tweaks` itself) and store it as the `RELEASE_PLEASE_TOKEN` secret, then uncomment `.github/workflows/release-please.yml`'s `token:` line — without it, a release-please-created GitHub Release uses the default `GITHUB_TOKEN`, which does not trigger other workflows, so `release: published` stays silent and item 1's mirror never fires regardless of whether `MARKETPLACE_TOKEN` is set (reason-not-auto: requires-signoff, same as item 1 — found during this task's whole-branch review, not the original plan) | open | — |
