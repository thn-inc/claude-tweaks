# Open Items — Release notes: authoring contract (#2580) + local-merge rendering (#2581)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `.claude/skills/shared-contract-extraction/SKILL.md`'s "cite, don't restate" convention (Project conventions) covers new prose restating a `_shared` contract; #2580 hit the adjacent, undocumented case of two *pre-existing* prose restatements (`shaping-mode.md`, `capture/SKILL.md`) going stale when the cited contract's own definition grew a section — deferred to a follow-up (#2589) rather than fixed in-change, since neither restatement blocks #2580's own ACs | observation | Filed #2589; not itself a divergence from the skill's documented convention, but a case worth the skill capturing explicitly if it recurs |
| 2 | review/3c-b (Error Handling) | Initial finding: `plugin/bin/lib/issues/materialize-format.js`'s `REQUIRED_SECTIONS` (the Materialization hard gate `bin/materialize.js` uses to admit a record for `/build`) was never extended with `## Release Note`, unlike the sibling `plugin/bin/lib/compose-record/compose.js` module. On tracing this against #2580's own issue body (not just its plan), determined this is **not a gap**: #2580's Key Files/Deliverables/ACs name only `compose-record/compose.js` (AC6) — never `materialize-format.js` — and its Gotchas paragraph is explicit that rollout friction "is expected, not a bug to design around," hits "at merge time" via `compose-subject.js`, and that in-flight records should still be buildable, only blocked at the final merge gate until re-shaped. Extending the build-time gate too would contradict that documented two-stage design (block at merge only, not at materialize). | resolved — no action | `staged/review-1.patch` (which had implemented+verified the now-rejected fix) deleted before console review; ledger corrected in place rather than left staged, since the finding was investigated and disproved against the primary spec text before the Wrap-Up Review Console ran |
| 3 | review/3c-b, related | Same root cause as #2, same disposition: `specShapedBody` (`plugin/bin/lib/issues/record.js`, shared by the four health-sweep skills' born-`ready` issue-payload.js callers) has no `releaseNote` support, so health-sweep-filed records will hit `compose-subject.js`'s new unconditional gate at merge time. This is exactly the "any hand-authored merge that bypasses `/specify`'s spec template" case #2580's own Gotchas paragraph names as accepted friction, "no escape hatch" — not something #2580 is scoped to fix. No follow-up issue filed: the spec text already documents this consequence explicitly, so there is nothing new to record. | resolved — no action | Investigated (`specShapedBody` in `plugin/bin/lib/issues/record.js`, 4 callers under `plugin/bin/lib/{code,docs,harness,journey}-health/issue-payload.js`), no code changed |
| 4 | review/3b-a + 3c-b (newline injection) | Initial finding: `composeSubject`'s `releaseNote` handling has no guard against embedded newlines/trailer-injection in the value. Determined **not a gap**: #2580's own Gotchas paragraph states explicitly "No write-side truncation happens either... it writes cleanReleaseNote verbatim... exactly as migrationNote/BREAKING CHANGE: already does today" — confirmed by reading `migrationNote`'s existing validation in `subject.js`, which has the identical `.trim()`-only shape with no newline guard. Adding a guard would diverge from documented, existing precedent rather than fix a real gap. | resolved — no action | No code changed; verified via direct read of `subject.js`'s `migrationNote` validation (lines ~57-64) |
| 5 | review/3c-b (prose restatement) | `plugin/skills/backlog/refine-mode.md`'s spec-shaped-body check (and the flag-back comment it builds) restates the pre-#2580 three-section definition — same class of gap as ledger item #1 (`shaping-mode.md`, `capture/SKILL.md`), already tracked as #2589. Not fixed in-change: consistent with item #1's precedent, and `refine-mode.md`'s check silently passing a Release-Note-only gap is still caught later at merge time by `compose-subject.js`'s unconditional gate — friction, not a silent failure. | observation | Folded into #2589 (retitled, body extended) rather than filing a duplicate or fixing in-change |
| 6 | review/3f-b | Suggested adding `releaseNote:` to `subject.test.js`'s "exempt" error-path tests for robustness. Checked empirically: every existing error-path `composeSubject(...)` call in `subject.test.js` throws before reaching the `releaseNote` validation (title/type/migrationNote checks all run first in argument-validation order) — there is no test that silently passes or breaks by omitting `releaseNote`. Full suite (9195/9195) already confirms this. No gap to fix. | resolved — no action | Verified via `awk`-driven scan of every `composeSubject(` call site in the test file cross-checked against `composeSubject`'s validation order in `subject.js` |
| 7 | review/3f-b | Low-severity fixture-realism suggestion, considered and below the calibration bar for this change — no ledger-worthy detail beyond this line. | dropped | n/a |
