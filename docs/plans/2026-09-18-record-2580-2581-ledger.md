# Open Items — Release notes: authoring contract (#2580) + local-merge rendering (#2581)

| # | Phase | Item | Status | Resolution |
|---|-------|------|--------|------------|
| 1 | build/skill | `.claude/skills/shared-contract-extraction/SKILL.md`'s "cite, don't restate" convention (Project conventions) covers new prose restating a `_shared` contract; #2580 hit the adjacent, undocumented case of two *pre-existing* prose restatements (`shaping-mode.md`, `capture/SKILL.md`) going stale when the cited contract's own definition grew a section — deferred to a follow-up (#2589) rather than fixed in-change, since neither restatement blocks #2580's own ACs | observation | Filed #2589; not itself a divergence from the skill's documented convention, but a case worth the skill capturing explicitly if it recurs |
